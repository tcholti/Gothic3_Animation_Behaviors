# Gothic 3 Animation Behaviors — Evidence Ledger

**Status:** Canonical evidence ledger  
**Date:** 2026-08-22

## 1. Purpose

This ledger prevents hypotheses becoming facts by repetition, design preferences being confused with engine behavior, and old prototype limitations being mistaken for architecture limits.

Statuses:

- **CONFIRMED**
- **STRONGLY SUPPORTED**
- **WORKING HYPOTHESIS**
- **UNKNOWN**
- **DESIGN DECISION**

## 2. Ledger

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-001 | CombatMove carries Self, Target, Action, PhaseName, and AniSpeedScale. | CONFIRMED | SDK + runtime logger | Core phase/speed/target diagnostic layer. |
| EV-002 | 2H normal Attack originally requests Hit at 0.700 without Raise. | CONFIRMED | controlled logger | Raise must be inserted if desired. |
| EV-003 | 1H normal Attack Hit was observed at 0.600. | CONFIRMED | controlled logger | Stock weapon families have different timing. |
| EV-004 | QuickAttack Hit was observed at 1.000. | CONFIRMED | controlled logger | Quick baseline differs from normal Attack. |
| EV-005 | PowerAttack uses native Raise before Hit; tested Raise 1.500, Hit 1.000. | CONFIRMED | controlled logger | Native PowerAttack is a reference sequence. |
| EV-006 | `PREPEND_BREAK_BLOCK` can insert an asynchronous Raise before original PS_Melee_Attack. | CONFIRMED | controlled prototype | Proven Raise architecture. |
| EV-007 | Engine resolves correct P0/P1 Raise automatically after action+phase request. | CONFIRMED | controlled prototype | Avoid exact filename construction. |
| EV-008 | Ordinary Recover visibly plays without appearing as a normal separate external CombatMove request in the original logger. | CONFIRMED | runtime | Recover can be internally initiated. |
| EV-009 | Controlled Recover should follow effective Hit speed. | DESIGN DECISION | project design | No planned user-facing RecoverSpeed tuning key. |
| EV-010 | `GetAnimationSpeedModifier` hook at Script_Game + 0x42A0 can observe/override action/phase speed. | CONFIRMED | prototype/source | Speed control feasible; compatibility still open. |
| EV-011 | Current upstream speed hook conflicts with NewBalance when both own the same path. | CONFIRMED | integration test | Production speed authority should not rely on unsafe hook competition. |
| EV-012 | Custom Blender frame-effect strings survive export into `.xmot`. | CONFIRMED | controlled frame-effect logger | Animation-authored marker channel is viable. |
| EV-013 | `UpdateFrameEffects` dispatches authored effects through `StartEffect`. | CONFIRMED | reverse engineering + runtime | Marker callback can execute code at authored frame. |
| EV-014 | `eSFrameEffect` tested layout is stride 8, frame at +0x00, name at +0x04. | CONFIRMED | runtime/reverse engineering | Used by current prototype; build-specific. |
| EV-015 | Exact current motion's frame-effect list can be inspected before marker fires. | CONFIRMED | v0.4/v0.5 prototype | Enables pre-marker ownership decision. |
| EV-016 | Marker presence can suppress native timed Normal activation for that exact execution. | CONFIRMED | v0.5 | Prevents native collision from activating before marker. |
| EV-017 | Marker firing can activate right weapon collision and clear triggered list. | CONFIRMED | v0.5 | Causal authored-frame collision proof. |
| EV-018 | Collision ownership follows marker presence rather than P0/P1. | CONFIRMED | reversal test | Do not encode pose as ownership. |
| EV-019 | Weapon collision naturally resets Item_Attack 7 -> Item_Equipped 5 at Hit -> Recover. | CONFIRMED | SetCollisionGroup hook | Normal single-hit attacks do not require end marker. |
| EV-020 | Marker timing remains synchronized to authored frame under playback-speed change. | CONFIRMED | slow/1.0 speed tests | Frame collision works with later speed tuning. |
| EV-021 | Marker-to-collision operation delay measured about 0.02–0.03 ms in tested path. | CONFIRMED | runtime logging | Callback latency is negligible relative to animation frame timing. |
| EV-022 | For the tested 2H normal attack authored from frame 0 through frame 12 inclusive (13 sampled frames), marker frame 8 felt better than frame 9 where first plausible contact occurred. | CONFIRMED FOR TESTED ATTACK | controlled animation test + clarified zero-based authoring convention | Useful authoring guideline only for that attack. |
| EV-023 | Collision should always start one frame before contact. | UNKNOWN | no broad test | Do not generalize frame-8 result globally. |
| EV-024 | Staff normal marker-controlled collision works on player. | CONFIRMED | v0.6 | Source resolver can support Staff item path. |
| EV-025 | Staff normal marker-controlled collision works on human NPC's own halberd. | CONFIRMED | v0.6 NPC test | System is not inherently player-only. |
| EV-026 | QuickAttack markers fire on Staff Quick animations. | CONFIRMED | runtime | Marker channel itself works for Quick. |
| EV-027 | v0.6 rejects Quick ownership because of Normal-specific `_Attack_Hit_` eligibility. | CONFIRMED | runtime/prototype logic | Replace filename-only eligibility with callback + exact action + Hit phase + exact-motion marker. |
| EV-028 | `OnAI_QuickAttack` is the relevant callback family for QuickAttackR/L variants. | CONFIRMED | Jackydima source/runtime action values | v0.7 should handle this callback. |
| EV-029 | Marked Fist tests can damage via left leg, right leg, right hand, and left hand with native timed activation suppressed. | CONFIRMED | controlled tests | Physical contact is not tied to a literal right-hand weapon. |
| EV-030 | Logical right-slot Fist entity remains collision group 0 when SetCollisionGroup(Item_Attack) is called. | CONFIRMED | runtime logging | Fist does not behave like normal weapon collision group. |
| EV-031 | Fist `SetCollisionGroup` is unnecessary for damage. | WORKING HYPOTHESIS | not yet isolated | Test by removing only group call while keeping ClearTriggeredList. |
| EV-032 | Triggered-list clearing is the critical Fist rearm operation. | STRONGLY SUPPORTED | current observations | Needs dedicated causal isolation. |
| EV-033 | Repeated multi-hit strikes need rearming/triggered-list clearing. | STRONGLY SUPPORTED | dual-power/whirl source + behavior | Production multi-hit helper should support repeated rearm. |
| EV-034 | `RIGHT / LEFT / BOTH / OFF` are final marker names. | UNKNOWN / PROPOSAL | current preferred design direction only | Keep generic source markers as candidates; do not mass-author yet. |
| EV-035 | Independent `OnAI_WhirlAttack` hooks from FrameCollision v0.19 and current `Script_AttackCollision` coexist without duplicate activation in the tested installation/load order. | CONFIRMED FOR TESTED MARKED 2H WHIRL CONFIGURATION; NOT GENERALIZED | combined New Balance + `Script_AttackCollision` commit `5f046b0` + v0.19 runtime log, 2026-08-24 | This is not proof that arbitrary same-function hooks or every load order chain safely. Preserve one authoritative collision owner and retest if DLL naming/load order or marker timing changes. |
| EV-036 | Human actors share Hero-family animations despite different body/armature object names. | STRONGLY SUPPORTED | extensive animation modding + NPC test | Hero must not be treated as player-only. |
| EV-037 | Composite Pxx pose labels are meaningful and must be preserved. | STRONGLY SUPPORTED | repeated asset/runtime behavior | Do not simplify pose identifiers. |
| EV-038 | Destination pose token controls/participates in resulting stance. | STRONGLY SUPPORTED | repeated renaming/mod tests | Preserve destination pose semantics. |
| EV-039 | Final `L/R` filename token is exact physical collision-source selector. | UNKNOWN | source says likely attack direction | Do not use as collision ownership authority. |
| EV-040 | `_00_%_00_` has a defined known gameplay meaning. | UNKNOWN | no established evidence | Treat as opaque. |
| EV-041 | Movement numeric suffixes are literal blend weights. | WORKING HYPOTHESIS | source speculation | Do not encode as confirmed engine rule. |
| EV-042 | Raw `gEUseType` must be normalized to animation tokens for animation-profile matching. | CONFIRMED AS MAPPING REQUIREMENT | supplied mapping + asset names | Axis of INI/runtime profile matching. |
| EV-043 | Missing configuration should leave native behavior untouched. | DESIGN DECISION | project safety principle | Required production fallback. |
| EV-044 | Unmarked animation should leave legacy/native collision ownership untouched. | DESIGN DECISION + PROVEN PROTOTYPE PATTERN | v0.5/v0.6 | Required frame-collision opt-in behavior. |
| EV-045 | Jackydima current WhirlAttack path sets `PropertyResetOnUntouch = GETrue`. | CONFIRMED | commit `da61a791...` | Relevant to repeated-contact Whirl behavior. |
| EV-046 | Exact Quick/QuickR/QuickL action values are required by the current stateless v0.7 marker path. | DESIGN DECISION | `OnAI_QuickAttack` scopes suppression earlier, but global `StartEffect` later lacks callback identity | Use exact action + Hit phase + exact marker to correlate the global marker; avoid per-execution state until needed. |
| EV-047 | Dual P0 Normal uses the left weapon as collision source. | STRONGLY SUPPORTED | animation-author empirical evidence + current third-party source behavior | Preserve when source-explicit markers/general resolver replace the right-hand prototype. |
| EV-048 | Dual P1 Quick uses the left weapon as collision source. | STRONGLY SUPPORTED | animation-author empirical evidence + current third-party source behavior | Left-source Quick support is required before marking this animation. |
| EV-049 | A Dual P3 Quick animation file exists and visually represents a left-weapon attack. | STRONGLY SUPPORTED | animation-author file/animation evidence | Catalog the exact file when the animation list is added. |
| EV-050 | Gothic 3 normally resolves/uses the Dual P3 Quick animation file in gameplay. | UNKNOWN | runtime use not confirmed | Do not claim native runtime use until observed. |
| EV-051 | Torch+1H P1 and P3 Quick attacks use the left torch as collision source. | STRONGLY SUPPORTED | animation-author empirical evidence; P1 also converges with third-party source | Left-source marker/resolver must cover both poses before marking these animations. |
| EV-052 | Some Torch+1H P0 Normal attacks natively activate left-torch collision, but that is unintended for those animations. | STRONGLY SUPPORTED | animation-author empirical evidence | Do not preserve the erroneous native left-source behavior as intended design. |
| EV-053 | Jackydima routes regular Torch+1H Normal collision to the right weapon. | CONFIRMED | current `Script_AttackCollision` source/comment | Useful correction/reference behavior for future source handling. |
| EV-054 | Dual P1 Pierce uses the left weapon as collision source. | STRONGLY SUPPORTED | animation-author empirical evidence + current third-party source behavior | Required future Pierce source case. |
| EV-055 | Jackydima's Dual Power path activates both equipped weapons and rearms both for later contact. | CONFIRMED | current `Script_AttackCollision` source | Good reference for eventual BOTH marker and multi-hit rearm. |
| EV-056 | Native/legacy Dual P0 Power may use only the left weapon. | CONTRADICTED FOR TESTED PLAYER SETUP | controlled unmarked v0.10 Dual log: P0 Power activated RIGHT and LEFT at the same state time | Treat tested P0 Power as BOTH-source. Preserve broader build/mod caveats rather than the old left-only recollection. |
| EV-057 | One Dual finishing animation may visually contact with both weapons while only one source damages; the exact native source is not yet known. | UNKNOWN | animation-author recollection not yet isolated | One damaging source is acceptable for that specific animation, but do not generalize. |
| EV-058 | `gCScriptRoutine_PS` exposes a two-valued `HitDirection` property (`Left`, `Right`). | CONFIRMED | official SDK enum + property-set declaration | Logical left/right combat state exists independently of physical collision geometry. |
| EV-059 | Attack/reaction R/L is a physical right-hand/left-hand collision-source selector. | CONTRADICTED | Torch+1H and Dual empirical source cases; both QuickAttackR/L can use the same left source | Never resolve collision hand from action R/L or final filename R/L. |
| EV-060 | Final attack R/L and R/L action variants correlate with logical attack/hit direction. | STRONGLY SUPPORTED | complete 5,991-name inventory; SDK `HitDirection`; Jackydima's disabled Normal pose mapping | Preserve as direction metadata for later analysis, not as current runtime authority. |
| EV-061 | The exact causal pipeline from action/final R/L through `Routine.HitDirection` to victim `StumbleR/L` is known. | UNKNOWN | correlation only; no controlled property-forcing test or native reaction source trace | No current plan depends on it; test only if a later feature needs it. |
| EV-062 | Hero QuickStumble is a systematic non-R/L family. | CONFIRMED AS INVENTORY OBSERVATION | 104 native Hero entries; P0/P1/P2/P3 patterns across 1H, Staff, Shield+1H, Torch+1H | A large visual lean or stance change does not imply an R/L reaction action. |
| EV-063 | Visible weapon travel determines Gothic 3's logical left/right hit direction at contact time. | CONTRADICTED | animation-author controlled visual reversal retained gameplay behavior | Treat direction as authored/selected combat state, not physics-derived motion analysis. |
| EV-064 | CombatMoveLogger v0.4 currently records `Routine.HitDirection`. | CONTRADICTED | current logger source audit | Optional future diagnostic: add HitDirection only if direction becomes relevant to implementation. |
| EV-065 | Native filenames contain `LightStumble`, but the SDK declares a separate `gEAction_LightStumble`. | CONTRADICTED | inventory + official `gEAction` enum | Do not assume every serialized action token is a 1:1 enum spelling; exact LightStumble mapping remains open. |
| EV-066 | FrameCollision v0.7 candidate implements marked Quick/QuickR/QuickL ownership through `OnAI_QuickAttack`, Hit phase, exact-motion marker, and the unchanged right-hand source resolver. | CONFIRMED AS SOURCE IMPLEMENTATION AND BUILD; RUNTIME FAILED | commit `04d12f8`; standalone build; controlled player Staff Quick log, 2026-08-22 | Initial QuickR/L activation waits for the marker, but post-Hit native reactivation prevents promotion. |
| EV-067 | FrameCollision v0.7 preserves marked player Staff Normal P0/P1 behavior: each execution suppressed the native callback, activated the right-hand Staff only at authored marker frame 5, cleared the triggered list, and naturally reset 7 -> 5. | CONFIRMED | controlled v0.7 runtime log, 2026-08-22; both executions were `Action: 1` and `_Attack_Hit_` | Normal regression path passed; this run does not validate Quick ownership. |
| EV-068 | For marked player Staff QuickAttackR (`Action: 4`) and QuickAttackL (`Action: 5`), v0.7 suppressed pre-marker native activation and accepted marker frame 6, but immediately after each natural 7 -> 5 reset the Staff was switched 5 -> 7 again without another marker. | CONFIRMED | controlled v0.7 runtime log, 2026-08-22 | v0.7 Quick ownership is incomplete and leaves collision active between attacks; do not ship or expand it. |
| EV-069 | The post-Hit Quick reactivation occurs because marker ownership leaves `Routine.PropertyStatePosition` at 0, allowing the original Quick callback to perform its one-shot timed activation later. | STRONGLY SUPPORTED | marker log shows `StatePosition: 0`; Jackydima `OnAI_QuickAttack` gates activation on 0 and sets 1 after activation; exact native callback trace not yet captured | Smallest next candidate: set StatePosition to 1 only when an accepted Quick marker performs activation, then retest. |
| EV-070 | The attempted moving Staff Quick selected P0 QuickAttackR/action 4 again rather than generic Quick/action 3. | CONFIRMED FOR TESTED INPUT | third execution in controlled v0.7 log | Generic Quick/action 3 remains runtime-untested; do not infer that moving always selects a separate action. |
| EV-071 | FrameCollision v0.8 sets `Routine.PropertyStatePosition` to 1 only when an accepted Quick marker activates/rearms collision, and logs its before/after values; Normal marker behavior is not mutated. | CONFIRMED AS SOURCE IMPLEMENTATION, BUILD, AND PLAYER RUNTIME | commit `f4d2946`; standalone Win32 Release build; controlled v0.8 player Staff log, 2026-08-22 | The narrow bookkeeping completion fixes the tested v0.7 Quick post-reset reactivation without regressing marked Normal behavior. |
| EV-072 | For marked player Staff QuickAttackR (`Action: 4`) and QuickAttackL (`Action: 5`), v0.8 accepted marker frame 6 with `StatePosition` 0 -> 1, performed the only 5 -> 7 activation, and naturally reset 7 -> 5 without a later unmarked reactivation. | CONFIRMED | controlled v0.8 runtime log, 2026-08-22 | Player QuickR/L regression is passed; proceed to NPC Staff Quick validation before expanding callback families or source meaning. |
| EV-073 | FrameCollision v0.8 preserves marked player Staff Normal behavior: marker frame 5 performs 5 -> 7 activation and the engine naturally resets 7 -> 5. | CONFIRMED | controlled v0.8 runtime log, 2026-08-22; `Action: 1` | Shared activation/reset core remains compatible with the separate Normal callback adapter. |
| EV-074 | FrameCollision v0.8 marker-controls human NPC Staff Quick attacks on the NPC's own equipped source: `OutNovice_01` produced two QuickAttackR/action 4 and five QuickAttackL/action 5 executions using `It_Halberd_01`; every marker changed StatePosition 0 -> 1, activated 5 -> 7, and naturally reset 7 -> 5 without immediate reactivation. | CONFIRMED | controlled v0.8 NPC runtime log, 2026-08-22 | QuickR/L ownership and the narrow bookkeeping fix are not player-specific; the right-hand prototype source resolves against the current actor. |
| EV-075 | Generic Quick/action 3 did not occur in the controlled v0.8 NPC Staff session. | CONFIRMED FOR TESTED SESSION | 16 accepted markers included Normal/action 1, QuickR/action 4, and QuickL/action 5 only | Generic Quick/action 3 remains unvalidated; do not infer support from QuickR/L results alone. |
| EV-076 | The official SDK declares raw `gEUseType_Fist = 8` and `gEUseType_PhysicalFist = 55`. | CONFIRMED | pinned official SDK `GameEnum.h` at `90bfd344...` | These raw UseTypes can scope the Fist-only causal branch without using animation names or collision group 0 as identity. |
| EV-077 | The initial FrameCollision v0.9 source attempted to call `GetUseType()` directly on the script-layer `Entity` wrapper. | CONFIRMED BUILD FAILURE | commit `89f36d8`; MSVC C2039 at all wrapper calls, 2026-08-22 | `Entity` does not expose `GetUseType()`; do not repeat this access pattern. The installed validated v0.8 DLL was not replaced. |
| EV-078 | The first v0.9 accessor correction called `GetUseType()` as an `eCEntity` member after `Entity.GetInstance()`. | CONFIRMED BUILD FAILURE | commit `9b4a73c`; MSVC C2039 on `eCEntity::GetUseType`, 2026-08-22 | `GetUseType()` is not an `eCEntity` member; it belongs to game-layer `gCEntity`. The installed v0.8 DLL remained untouched. |
| EV-079 | The second v0.9 correction passes the `eCEntity*` from `Entity.GetInstance()` to the SDK-declared static `gCEntity::GetUseType(eCEntity*)`, preserving the Fist-only skip and unconditional triggered-list clearing. | CONFIRMED AS SOURCE IMPLEMENTATION, BUILD, AND TESTED RUNTIME MATRIX | commit `11f2a1b`; pinned `gCEntity` SDK declaration; successful Win32 Release build; installed build hash `16B2F35D...F3140`; EV-080–EV-085 | The raw-UseType branch compiled and the complete tested Fist contact matrix passed. Runtime scope remains raw Fist UseType 8 on the player fixture; PhysicalFist 55 and monster bodies are not thereby proven. |
| EV-080 | In the v0.9 native-motion left-hand Fist baseline, the marked Hero attack damaged the target while the Hero's original Normal callback was suppressed; all eight accepted frame-3 markers resolved `Fist` UseType 8, skipped `SetCollisionGroup(Item_Attack)`, kept group 0 -> 0, and cleared the triggered list. | CONFIRMED FOR TESTED LEFT-HAND MOTION | controlled v0.9 log + animation-author visual damage observation, 2026-08-22 | The weapon-style group request is unnecessary for this tested Fist contact; EV-084 records the completed matrix. |
| EV-081 | In the v0.9 custom right-hand Fist variant, one focused neutral target contact visibly damaged the target while the accepted frame-3 marker resolved raw Fist UseType 8, skipped `SetCollisionGroup(Item_Attack)`, kept group 0 -> 0, and cleared the triggered list. | CONFIRMED FOR TESTED RIGHT-HAND MOTION | controlled v0.9 log + animation-author visual damage observation, 2026-08-22 | Together with EV-080, the weapon-style group request is unnecessary for the tested left- and right-hand Fist contacts; EV-084 records the completed matrix. |
| EV-082 | Fist contact against an unfocused allied human failed in two sessions even though the marked P0 path accepted 12 total markers and the unmarked P1 legacy/native path ran eight callbacks; a marked 2H attack against the unfocused allied setup activated 5 -> 7 and visibly connected. | CONFIRMED FOR TESTED TARGET SETUP; FIST CAUSE STRONGLY SUPPORTED | three controlled v0.9 logs + animation-author contact observations, 2026-08-22 | An unfocused allied target is invalid for Fist body-contact testing. The contrast supports Fist-specific target/focus gating rather than a universal inability to hit green-name NPCs. Use a focusable neutral target. |
| EV-083 | In the two repeated unfocused Fist sessions, accepted marked-P0 events outnumbered logged frame-controlled ownership decisions: 5 versus 4 in the first session and 7 versus 5 in the larger session. | CONFIRMED AS LOG OBSERVATION; CAUSE UNKNOWN | controlled v0.9 unfocused Fist logs, 2026-08-22 | Marker playback can recur without a fresh logged ownership decision in this input pattern. It does not invalidate the completed focused-target matrix, but the exact continuous-action/callback relationship should be investigated before production ownership assumes a strict 1:1 count. |
| EV-084 | The complete v0.9 player Fist contact matrix damaged a focused target with the native left hand and custom right hand, left leg, right leg, and head while every accepted marker resolved raw Fist UseType 8, skipped `SetCollisionGroup(Item_Attack)`, kept group 0 -> 0, and cleared the triggered list. Head and left leg used marker frame 3; right leg used marker frame 2. | CONFIRMED FOR THE FIVE TESTED CONTACT MOTIONS | five controlled v0.9 logs + animation-author visual damage observations, 2026-08-22 | The weapon-style group request is unnecessary for these tested hand/leg/head contacts. Treat Fist as a logical body-contact/rearm source, not a right-hand weapon group. Do not generalize to every body part, PhysicalFist 55, or monster bodies without evidence. |
| EV-085 | The focused-neutral right-hand, left-leg, right-leg, and head logs unload immediately after their accepted marker, and the tester may have exited before each custom Recover completed. | CONFIRMED AS TEST LIMITATION | four controlled v0.9 logs + tester report, 2026-08-22 | These sessions prove marker-path contact but do not prove Recover completion or a Hit -> Recover cleanup/reset. No repeat is required for the Fist group-call causality question because the logical Fist group remained 0 and damage already occurred. |
| EV-086 | The custom right-leg Fist fixture used collision on frame 2 and whoosh on frame 3 because those two authored effects were accidentally swapped. | CONFIRMED AS FIXTURE AUTHORING NOTE | Blender inspection + animation-author report, 2026-08-22 | Do not interpret frame 2 as an engine requirement. The positive contact still supports the Fist group-call result. |
| EV-087 | In pinned Jackydima `Script_AttackCollision`, Dual P0 Normal uses left; Dual P1 Pierce, Quick, and SimpleWhirl use left; Torch+1H P1 Quick uses left; other cases in those callbacks use right; Dual Power activates both and later clears both lists again; Whirl activates right and enables `ResetOnUntouch`. | CONFIRMED AS THIRD-PARTY SOURCE BEHAVIOR | `scripts/Script_AttackCollision/Script_AttackCollision.cpp` at `da61a791...` | Use as a reference map and test target, not as proof that every native/game build follows the same behavior. |
| EV-088 | Collision expansion should cover the main human melee callback families before production integration, while initial Raise and playback-speed scope remains Normal and Quick. | DESIGN DECISION | project planning, 2026-08-22 | Research source/BOTH/rearm/OFF semantics broadly for collision without forcing the same scope onto Raise/speed. |
| EV-089 | Gothic 3 animations in this project are authored starting at frame 0; a range from 0 through N inclusive contains N + 1 sampled frames. Previous shorthand often used the ending frame number as the nominal frame count. | CONFIRMED AUTHORING CONVENTION | animation-author clarification, 2026-08-22 | Record both the inclusive range and sampled-frame count when duration/rescaling matters; ask for clarification when only a bare count is supplied. Existing marker frame numbers remain literal indices. |
| EV-090 | FrameCollision v0.10, which preserves v0.9 behavior and adds passive exact player-slot identity diagnostics, compiles successfully as Win32 Release, its installed DLL matches the build, and its slot labels passed the controlled player Dual discovery session. | CONFIRMED AS SOURCE IMPLEMENTATION, BUILD, INSTALL, AND TESTED DIAGNOSTIC | local MSBuild/install + controlled unmarked player Dual log, 2026-08-22; build/install SHA-256 `647B8C36...B0C6D`; v0.9 rollback `16B2F35D...F3140` preserved | Exact entity-pointer slot matching is suitable for current player source discovery; NPC ownership association remains outside this player-specific diagnostic label. |
| EV-091 | In the controlled unmarked player Dual session, P0 Normal used LEFT; P1 Normal used RIGHT; P0 QuickAttackR and QuickAttackL both used RIGHT; P1 QuickAttackR and QuickAttackL both used LEFT; P0 Pierce used RIGHT; P1 Pierce used LEFT; P0 and P1 Power each activated RIGHT and LEFT. | CONFIRMED FOR TESTED PLAYER DUAL EXECUTIONS | v0.10 exact slot-identity log, 2026-08-22; 15 attacks plus two Block prerequisites | This is the runtime source map for the tested Dual Normal/Quick/Pierce/Power paths. Implement source-explicit markers/adapters to reproduce it deliberately. |
| EV-092 | Every one of the 17 logged Dual weapon activations (seven Normal, four Quick, two Pierce, and two sources for each of two Power attacks) had a matching 7 -> 5 reset during phase 3. | CONFIRMED FOR TESTED SESSION | v0.10 log; 17 activation records and 17 reset records | Ordinary group cleanup worked for all tested source paths, including both Pierce attacks and both weapons in each Power attack. Pierce Recover is known-good and full visual Recover completion is outside this test's scope. |
| EV-093 | Dual QuickAttackR/L action or filename suffix does not select the physical weapon hand: both P0 variants used RIGHT and both P1 variants used LEFT. | CONFIRMED FOR TESTED DUAL QUICK EXECUTIONS | v0.10 exact slot-identity log, actions 4 and 5 in both poses | Resolve authored collision source independently of Quick R/L direction metadata; reinforces EV-059. |
| EV-094 | P0 and P1 Dual Power each issued separate RIGHT and LEFT 5 -> 7 transitions at the same logged state time, then reset both to 5 together in phase 3. | CONFIRMED FOR TESTED POWER EXECUTIONS | v0.10 exact slot-identity log | BOTH group activation/reset is established. Later triggered-list re-clearing/repeated-contact behavior was not observed by this group-only diagnostic and remains separate work. |
| EV-095 | Dual Power visually contains three weapon contacts: P0 is right -> left -> right; P1 is left -> right -> left. | STRONGLY SUPPORTED ANIMATION-AUTHOR EVIDENCE | direct Blender/gameplay observation, 2026-08-22 | BOTH initial activation is insufficient by itself for three same-target hits because the first weapon must be rearmed before its second contact. |
| EV-096 | Native Dual Power commonly damages only twice despite three visual contacts because the first weapon's triggered list is not correctly rearmed for its second contact. | STRONGLY SUPPORTED; EXACT NATIVE CAUSE TO VERIFY | animation-author gameplay observation + v0.10 BOTH group result + TouchDamage list semantics | Next diagnostic must observe `ClearTriggeredList`; do not equate BOTH group activation with complete multi-hit behavior. |
| EV-097 | Jackydima's Dual Power callback activates/clears both sources initially and later clears both triggered lists again at the Dual-specific threshold. | CONFIRMED AS THIRD-PARTY SOURCE; FIX EFFECT STRONGLY SUPPORTED | pinned `Script_AttackCollision` source + animation-author NewBalance/collision-mod observation | This later rearm plausibly enables the third visual strike; isolate exact runtime hit/rearm timing before reproducing it. |
| EV-098 | Human Dual wield has a gameplay SimpleWhirl but no separate full Whirl; its assets serialize `WhirlAttack` in the filename. Human 2H and Staff have full Whirl; ordinary 1H families have no Whirl. Hand-to-hand coverage remains unknown. | STRONGLY SUPPORTED ANIMATION-AUTHOR/ASSET EVIDENCE | exact supplied animation names + gameplay input observations, 2026-08-22 | Do not plan a nonexistent Dual full-Whirl adapter. Log native action/callback identity rather than deriving it solely from the filename token. |
| EV-099 | Dual SimpleWhirl is triggered by holding attack slightly less than Power; 2H/Staff Whirl is Block + quick attack; 2H/Staff Finishing is Block + held attack. On a downed enemy, Finishing death timing is timer-based rather than collision-based and is insensitive to Raise-length changes; the timer appears to begin with Hit. | MIXED: INPUT/DEATH-TIMER BEHAVIOR STRONGLY SUPPORTED; STANDING HACK SELECTION WORKING HYPOTHESIS | repeated animation-author gameplay and length-edit tests | Preserve Finishing Hit timing when replacing assets; keep collision work separate from execution-death timing; verify standing Hack selection only if needed. |
| EV-100 | Repeated source-explicit markers should rearm their named source at each authored contact; `BOTH` means both sources at that moment rather than all future contacts, and OFF is reserved for deliberate inactive gaps. | DESIGN DECISION / PROPOSED MARKER SEMANTICS | Dual Power three-contact requirement + current marker architecture | Use P0 RIGHT/LEFT/RIGHT and P1 LEFT/RIGHT/LEFT as the reference multi-hit design; keep names provisional until runtime validation. |
| EV-101 | Pinned NewBalance independently contains `FixDualOneHanded`, installed at `RVA_ScriptGame(0x482e7)`, which clears the triggered lists of both equipped 1H weapons. | CONFIRMED AS THIRD-PARTY SOURCE BEHAVIOR | `scripts/Script_NewBalance/CallHook.cpp` at `da61a791...` | The Dual Power rearm fix exists in NewBalance as well as `Script_AttackCollision`; do not attribute it only to the collision DLL. The exact native call-site timing still requires runtime observation. |
| EV-102 | The underlying trigger property set exposes per-entity visit state through `EntitiesVisited` and `EntitiesVisitedCount`, plus both `ClearTriggeredList()` and `ClearTriggeredList(eCEntity *)`. | CONFIRMED AS SDK STRUCTURE | pinned `eCTrigger_PS` declaration at `da61a791...` | Strongly supports distinguishing "hit a different target in the same window" from "re-hit an already visited target." Runtime behavior and default count limits still require controlled tests. |
| EV-103 | With the tester's NewBalance-enabled setup, one Normal or Quick weapon swing can damage several opponents when the physical weapon reaches each of them; whether the same occurs natively is not remembered. | CONFIRMED AS TESTER OBSERVATION; CAUSAL OWNER/NATIVE BASELINE OPEN | repeated gameplay observation, 2026-08-22 | Do not assume NewBalance causes the multi-target behavior. Compare a native unmarked baseline and log target identities before changing triggered-list policy. |
| EV-104 | Explicit OFF is intended to create a deterministic inactive gap in authored multi-contact attacks, especially custom 2H/Staff double attacks where the second swing can otherwise touch a nearby target before acceleration. Rearming remains a source/contact-marker responsibility. | DESIGN DECISION / USER REQUIREMENT | custom double-attack gameplay observation + source-explicit marker design | OFF should deactivate marker-owned collision sources without itself inventing another hit; the following source marker reactivates and clears the appropriate list. Keep it generic across attack families if runtime ownership tracking proves stable. |
| EV-105 | The proposed marker vocabulary does not include a separate generic ON marker: RIGHT/LEFT/BOTH each mean activate plus rearm their named source set, while OFF closes the marker-owned window. | DESIGN CLARIFICATION | animator-controlled example, 2026-08-22 | A same-source double attack can use RIGHT -> OFF -> RIGHT; the second RIGHT clears the visited-target list again, allowing correctly timed contact with the same or different targets. |
| EV-106 | In the controlled v0.10 marked 2H Normal test with NewBalance and `Script_AttackCollision` absent, one frame-controlled collision window damaged multiple distinct wolves: one observed swing hit three and at least two observed swings hit two. Four player attacks produced exactly four accepted frame-7 markers, four right-weapon 5 -> 7 activations/list clears, and four natural 7 -> 5 resets in phase 3, with no extra player weapon activation. | CONFIRMED FOR TESTED PLAYER 2H/WOLF SETUP | archived v0.10 log + animation-author visual damage observation, 2026-08-22 | A single source-marker clear does not restrict the active weapon window to one target. This proves project marker-path multi-target behavior, not completely unmodified native behavior or same-target rearming. |
| EV-107 | The controlled same-motion 2H Normal comparison proved same-target rearming: native/no-marker and one-marker variants damaged the target once per attack, while the frame-4 plus frame-15 marker variant damaged the same target at both intended contacts. | CONFIRMED FOR TESTED PLAYER 2H NORMAL SETUP | three archived v0.10 logs + animation-author visual hit observations, 2026-08-22 | Re-clearing the same source at the later marker enables another same-target hit. OFF is not required for rearm; it remains responsible for closing an inactive gap. |
| EV-108 | In all four attacks of the two-marker variant, the authored frame-15 marker dispatched twice at the same logged `StateTime`; each attack therefore produced one early clear plus two back-to-back late clears, although only two visual damage contacts occurred. | CONFIRMED AS REPEATED RUNTIME LOG PATTERN | archived two-marker v0.10 log, 2026-08-22 | Add exact same-update marker deduplication before production. Current duplicate clears were harmless, but should not be treated as two authored contacts. |
| EV-109 | The animation author normally places collision markers one authored frame before intended visual contact because exact-contact placement can appear to begin after the weapon has entered the target. | CONFIRMED AUTHORING PREFERENCE; ENGINE CAUSE UNPROVEN | repeated visual authoring judgement + current double-contact test | Preserve marker timing as authored data; do not hardcode a universal engine offset. In the test fixture, markers/whooshes at frames 4 and 15 led intended visual contacts by one frame. |
| EV-110 | FrameCollision v0.11 adds a narrow duplicate-dispatch guard before any collision-group or triggered-list mutation. It suppresses only an identical actor, source, motion, marker, action, phase, and state-time tuple repeated within 5 ms; a later authored marker at a different state time remains eligible. | CONFIRMED AS SOURCE IMPLEMENTATION, BUILD, AND INSTALL | source commit `0bbc377`; successful Win32 Release build/install, 2026-08-22; build/install SHA-256 `F47EAD5B...D154`; v0.10 rollback SHA-256 `647B8C36...B0C6D` | The guard is actor/source/execution-specific rather than a general cooldown. Runtime behavior is recorded separately in EV-111. |
| EV-111 | In six v0.11 executions of the exact frame-4/frame-15 2H Normal fixture, every attack retained two genuine accepted contacts, ignored exactly one repeated late callback, cleared the list exactly twice, and naturally reset once. All ignored callbacks had zero state-time delta and arrived 0.0574–0.1162 ms after the accepted late callback; none requested collision or cleared the list. Two-contact behavior was also observed against two targets. | CONFIRMED FOR TESTED PLAYER 2H NORMAL SETUP | archived v0.11 duplicate-guard log + animation-author visual observations, 2026-08-22 | v0.11 solves the deterministic same-update duplicate without suppressing the genuine later rearm marker. Proceed to isolated OFF-marker work; retain a Quick repeated-marker regression as optional family-specific coverage. |
| EV-112 | FrameCollision v0.12 introduces provisional `G3AB_COL_OFF_TEST` for weapon-style right-hand sources. OFF closes only a matching window previously opened by `G3AB_COL_TEST` for the same actor/source/motion/action/phase, requests `Item_Equipped`, never clears the triggered list, and is a logged no-op before ON or after natural reset. Fist/body OFF remains unsupported. | CONFIRMED AS SOURCE IMPLEMENTATION, BUILD, INSTALL, AND TESTED OFF MECHANISM; REPEATED-MARKER REPLAY DEFECT OPEN | source commit `685bbb7`; successful Win32 Release build/install, 2026-08-22; build/install SHA-256 `F268FEDB...758B`; v0.11 rollback SHA-256 `F47EAD5B...D154`; v0.12 runtime logs, 2026-08-23 | OFF physically shortens a weapon window and later ON restores it, but v0.12 must not be promoted unchanged because interleaved marker replay bypasses its last-only dedupe guard. |
| EV-113 | The controlled horizontal 2H sweep proves physical OFF timing against distinct targets. ON-only produced three clean ON/reset cycles and could damage all three wolves when crossed. ON-f7/OFF-f9 produced seven clean ON/OFF cycles and usually hit one wolf, sometimes two when tightly grouped. ON-f7/OFF-f8 produced ten clean ON/OFF cycles, never damaged more than one wolf, and occasionally missed due to positioning. Every accepted OFF changed the right weapon 7 -> 5 and performed no list clear. | CONFIRMED FOR TESTED PLAYER 2H/WOLF SETUP | three archived v0.12 horizontal-sweep logs + animation-author visual observations, 2026-08-23 | OFF closes the active weapon window against later new targets. Earlier OFF narrows the physical contact interval further. This is separate from same-target visit-list rearming. |
| EV-114 | In the ON-f4/OFF-f10/ON-f15 double attack, both intended swings still damaged one or multiple targets, but every execution logged a late same-state `ON, OFF, ON` batch. Four single-target executions produced 12 accepted ON, eight accepted OFF, 12 clears, 12 right-source 5 -> 7 operations, and 12 total 7 -> 5 operations; three multi-target executions produced 9/6/9/9/9 respectively. No duplicate was ignored. | CONFIRMED RUNTIME REPLAY PATTERN AND PHYSICAL RESTORATION; V0.12 DEDUPE INSUFFICIENT | two archived v0.12 double-attack logs + animation-author visual observations, 2026-08-23 | The authored motion contains two ON, one OFF, and two whooshes. The late update replays prior marker names around the genuine late ON. Last-accepted-only dedupe fails when OFF is interleaved, causing three activations and two explicit OFF operations per attack even though final ON preserves visible second-contact behavior. |
| EV-115 | FrameCollision v0.13 combines two guards: retain v0.11 exact same-update identical-marker suppression, then enforce cached authored ON/OFF occurrence counts for the current actor/source/motion/action/phase execution. State-time rollback or an ownership-ending natural reset starts a fresh execution. | CONFIRMED AS SOURCE IMPLEMENTATION, BUILD, AND INSTALL | source commit `7a0f1b7`; successful Win32 Release build/install, 2026-08-23; build/install SHA-256 `0E4A50AD...1D692`; v0.12 rollback SHA-256 `F268FEDB...758B` | The first layer prevents an immediate duplicate ON from consuming the budget reserved for a later genuine ON; the second rejects interleaved replay. Counts are cached once per exact animation and checked only on reserved marker dispatch; there is no per-frame actor/world scan. Runtime behavior is recorded separately in EV-116. |
| EV-116 | In seven v0.13 executions of the ON-f4/OFF-f10/ON-f15 2H Normal fixture, every attack independently reset its execution budget and produced exactly two accepted ON/list clears, one accepted OFF/no-clear, one replayed OFF rejected by the authored-occurrence budget, one final ON rejected by the same-update duplicate guard, and one natural 7 -> 5 cleanup. Rejected callbacks requested no collision change and no list clear. Both intended swings visibly damaged each of two independently tested targets on their first attack; each target died on the first swing of the following attack. | CONFIRMED FOR TESTED PLAYER 2H NORMAL SETUP; PERFORMANCE NOT YET PROFILED | archived v0.13 occurrence-guard log + animation-author visual observations, 2026-08-23 | The combined guard resolves the v0.12 interleaved replay defect without suppressing genuine repeated contact. The event-driven/cached design has no per-frame actor/world scan, but large-battle performance remains a separate production validation rather than an inference from this functional test. |
| EV-117 | In four v0.13 2H Quick executions of the same ON-f4/OFF-f10/ON-f15 double fixture, three QuickAttackR/action 4 and one QuickAttackL/action 5 each independently reset the execution budget, accepted two ON/list clears and one OFF/no-clear, rejected one replayed OFF by occurrence budget, rejected the final repeated ON by the same-update duplicate guard, and naturally reset once. Each first ON changed Quick StatePosition 0 -> 1 and each second genuine ON preserved 1 -> 1. The targeted QuickAttackL damaged the opponent on both intended swings. | CONFIRMED FOR TESTED PLAYER 2H QUICK R/L SETUP | archived v0.13 Quick occurrence-guard log + animation-author visual observation, 2026-08-23 | The combined guard and Quick one-shot bookkeeping coexist correctly across genuine repeated contacts. The optional Quick family regression is complete; actual Whirl still requires its own callback adapter. |
| EV-118 | Known melee attacks cancel locomotion and play their own non-overlay action, which may include authored forward movement such as a short step. Actions that genuinely preserve movement-key control use separate `_O_` overlay resources, commonly upper-body motion layered over locomotion; native 2H `HoldRight` has paired `_N_` and `_O_` Begin/End files. | CONFIRMED ANIMATION-AUTHOR OBSERVATION; EXACT ENGINE REPRESENTATION NOT YET SOURCE-VERIFIED | extensive in-game animation work + supplied exact 2H HoldRight filename pairs, 2026-08-23 | Do not describe Quick/Normal melee as moving attacks merely because the request can interrupt locomotion while a movement key remains held. Preserve `_N_`/`_O_` as useful serialized semantics but use source action/phase authority in code. |
| EV-119 | For observed 2H and Staff behavior, movement resets the visible attack pose to P0, but the next P0 Quick may select QuickAttackL or QuickAttackR; returning to P0 through a Normal attack selected QuickAttackR again in the tested sequence. A few 1H observations appeared to alternate R/L, but coverage is insufficient. | CONFIRMED FOR DESCRIBED AUTHOR OBSERVATIONS; SELECTION ALGORITHM UNKNOWN | animation-author controlled observation outside the preserved v0.13 log, 2026-08-23; the log independently contains three P0 QuickAttackR and one P0 QuickAttackL execution | Do not infer a universal R/L alternation or pose-only selector. Marker ownership remains exact-motion plus callback/action/phase and does not require solving the selection algorithm. |
| EV-120 | No Raise played during the v0.13 2H Quick collision regression. This matches the current implementation: custom Raise insertion is proven/enabled for the Normal proof path but has not yet been generalized to Quick. Existing 2H/Staff Quick Raise assets remain future integration targets. | CONFIRMED CURRENT IMPLEMENTATION STATE | animation-author observation during controlled v0.13 Quick test, 2026-08-23; current project source/design scope | Do not treat the absent Raise as a collision regression. Implement and validate Quick Raise separately when returning to Raise/speed integration. |
| EV-121 | Current Jackydima source activates a left weapon by resolving `gESlot_LeftHand`, requesting `Item_Attack`, and clearing that entity's triggered list; Dual Power applies the same operations to both equipped slot entities, and NewBalance `FixDualOneHanded` clears right and left lists separately. | CONFIRMED SOURCE EVIDENCE | pinned `references/jackydima-gothic3sdk` `Script_AttackCollision.cpp` and `Script_NewBalance/CallHook.cpp`, inspected 2026-08-23 | Model LEFT as the same per-item operation used by RIGHT. Model BOTH as a coordinated two-source set, not as a new collision primitive or singular source. |
| EV-122 | The agreed LEFT-first prototype preserves `G3AB_COL_TEST` as RIGHT, adds provisional `G3AB_COL_LEFT_TEST`, gives RIGHT/LEFT exact-set semantics, uses fixed right/left slot snapshots plus a two-bit active mask, and keeps separate RIGHT/LEFT/OFF occurrence budgets. Missing any source required by the exact motion leaves the original callback active. BOTH is deliberately disabled until isolated LEFT and preserved-RIGHT runtime validation pass. | DESIGN DECISION / SOURCE CANDIDATE; RUNTIME UNVALIDATED | project design agreement + v0.14 candidate source, 2026-08-23 | Build and test LEFT on a known native left-source Dual Normal motion, then regress the existing RIGHT fixture. Only then enable BOTH without changing the state model. |
| EV-123 | FrameCollision v0.14 compiled successfully as Win32 Release and was installed byte-for-byte over validated v0.13. | CONFIRMED BUILD AND INSTALL | source commit `918c02d`; successful user build/install, 2026-08-23 | v0.14 build/install SHA-256 is `F52BBC58258C1F10EFE0BB35AC28F6B8208F2C65ADA3B3D9B48795F1B4ED88EB`; validated v0.13 rollback SHA-256 is `0E4A50AD5ED6F7D641FD23503298011F4CE7943628AC09AC3D257F075FC1D692`. |
| EV-124 | In the mixed v0.14 Dual session, two marked P0 Normal executions each owned one authored frame-4 LEFT marker, activated only the left weapon 5 -> 7, cleared its triggered list once, and naturally reset left 7 -> 5. Unmarked P1 remained legacy/native and could activate the right weapon. All tested attacks visibly damaged their targets. | CONFIRMED FOR TESTED PLAYER DUAL P0 NORMAL SETUP | archived P0-only v0.14 log + animation-author visual observations, 2026-08-23 | LEFT works without changing the unmarked native P1 path. No duplicate or occurrence-budget rejection appeared. |
| EV-125 | In the stronger v0.14 replacement session, six marked P0 and five marked P1 Dual Normal executions each accepted exactly one frame-4 LEFT marker. The 11 accepts produced exactly 11 left 5 -> 7 activations/list clears and 11 natural left 7 -> 5 resets, with zero player RIGHT collision-group requests. All attacks visibly damaged their targets. | CONFIRMED FOR TESTED PLAYER DUAL P0/P1 NORMAL SOURCE OVERRIDE | archived P0/P1 v0.14 log + animation-author visual observations, 2026-08-23 | This proves that exact-motion LEFT ownership replaces native P1 RIGHT activation rather than adding LEFT beside it. There were no duplicate or authored-budget rejections and no delayed player reactivation. Preserved RIGHT regression remains the gate before enabling BOTH. |
| EV-126 | The v0.14 preserved-RIGHT regression repeated the validated 2H Normal ON-f4/OFF-f10/ON-f15 fixture four times. Every execution reset its budget, accepted two RIGHT/list clears and one OFF/no-clear, rejected one replayed OFF by authored budget, rejected one final RIGHT by same-update dedupe, and produced the exact right-source `5 -> 7 -> 5 -> 7 -> 5` sequence with no LEFT activation. The targeted execution visibly damaged on both intended swings. | CONFIRMED FOR TESTED PLAYER 2H NORMAL RIGHT PATH | archived v0.14 RIGHT-regression log + animation-author visual observation, 2026-08-23 | The v0.14 two-slot refactor preserves RIGHT, OFF, rearm, occurrence protection, and duplicate protection even when the left slot is absent. LEFT and RIGHT gates are both complete. |
| EV-127 | Real Dual Power remains outside v0.14 ownership. All 45 Power-context reserved-marker callbacks in the single-target probe and all 55 in the multi-target probe were consumed as `REJECTED - unsupported Normal/Quick Hit context`, causing no marker mutation. Native Power independently activated and reset both player weapon slots for nine completed P0/P1 executions in the first log and eleven in the second. Normal attacks used for stance changes produced the only accepted custom markers. | CONFIRMED CALLBACK-GATE AND NATIVE GROUP-SOURCE EVIDENCE; THREE-CONTACT DAMAGE REMAINS VISUAL ONLY | two archived v0.14 Dual Power probe logs + animation-author observations, 2026-08-23 | Do not attribute the observed Power contacts to custom markers. Reuse the motion under a Normal filename to validate alternating exact sets and later BOTH; add actual Power callback ownership separately. A future target/damage observer should confirm per-contact damage. |
| EV-128 | The v0.14 Dual Normal alternating-source fixture produced 31 marked forward ownership decisions: 16 P0 RIGHT/LEFT/RIGHT and 15 P1 LEFT/RIGHT/LEFT. Twenty-nine executions completed all three accepted contacts with exact-set retirement and rearm; each complete P0 used RIGHT -> LEFT -> RIGHT and each complete P1 used LEFT -> RIGHT -> LEFT. The occurrence and same-update guards rejected the expected replay callbacks. Repeated targeted attacks visibly damaged the same opponent on all three intended contacts, including the first source's second contact, which native Dual Power is reported not to rearm. One accidental unmarked P0 side Normal remained legacy/native. | CONFIRMED FOR COMPLETE PLAYER DUAL NORMAL EXECUTIONS; INTERRUPTION SAFETY DEFECT FOUND | archived alternating-source log + animation-author visual observation, 2026-08-23 | Two executions were interrupted. One stopped after its first marker. In the other, action changed to 59 and the first source naturally reset, but a later RIGHT marker was accepted because Normal marker eligibility checked only the stale `_Attack_Hit_` movement-animation name. Before BOTH, require exact Normal action 1 plus Hit phase at marker time and retest interruption; keep completed exact-set/rearm evidence. |
| EV-129 | The v0.15 source candidate strengthens the shared Normal callback/marker predicate from filename-only `_Attack_Hit_` detection to the conjunction of `gEAction_Attack`, `gEPhase_Hit`, and the current `_Attack_Hit_` motion. Quick eligibility, exact-motion marker ownership, source resolution, source-set transitions, occurrence budgets, duplicate protection, and collision/list operations are unchanged. | CONFIRMED AS NARROW SOURCE IMPLEMENTATION; BUILD AND RUNTIME PENDING | `Script_FrameCollisionTest.cpp` source audit + official SDK enum declarations, 2026-08-23 | Build v0.15, preserve one complete alternating P0/P1 execution, and induce a terrain-slide/damage interruption before a later marker. The late marker must be rejected without collision or list mutation before BOTH is enabled. |
| EV-130 | FrameCollision v0.15 compiled and installed byte-for-byte. Across the two interruption sessions, 21 complete Dual Normal executions preserved the exact P0 RIGHT -> LEFT -> RIGHT and P1 LEFT -> RIGHT -> LEFT sequences. Damage reactions entered actions 26, 29, and 31 and stopped later attack markers; every reserved marker that did arrive was still in valid Normal action 1 or Quick action 4/5 plus Hit phase. No post-interruption marker mutation occurred. | CONFIRMED BUILD, INSTALL, AND REGRESSION; DIRECT INVALID-ACTION REJECTION BRANCH NOT OBSERVED | source commit `b19478b`; two archived v0.15 interruption logs; build/install SHA-256 `20DF9146...D85DED`; animation-author observations, 2026-08-23 | The v0.14 action-59-plus-late-marker scenario was not reproduced, so do not claim direct runtime proof of the new rejection branch. The shared action/phase guard is source- and build-confirmed and the valid Normal/Quick paths regressed safely. |
| EV-131 | One damage-interrupted 2H Normal exposed an independent occurrence-budget lifecycle defect. Its first RIGHT marker reset and consumed occurrence 1/2, then the weapon naturally changed 7 -> 5 at attack state time 0 while the stale action/phase/motion still matched. The next new attack's first RIGHT arrived at a slightly later marker time, was accepted as occurrence 2/2 with `ExecutionBudgetReset: 0`, and its later genuine RIGHT was rejected as budget-exhausted. | CONFIRMED FOR TESTED PLAYER 2H NORMAL INTERRUPTION | archived v0.15 target-damage interruption log, 2026-08-23 | State-time rollback observed only at the next marker cannot distinguish a new execution when an earlier attack ended before reaching a later marker. Before BOTH, give natural marker-owned source retirement a reliable execution-boundary signal while preserving explicit OFF and same-execution source switching. |
| EV-132 | The v0.16 source candidate adds two event-driven execution-boundary observations without actor/world scanning: controlled Normal/Quick callback state-time rollback retires the prior actor budget/dedupe record, and natural retirement of a still marker-owned weapon source also retires it when current state time rolled behind the last accepted marker. Explicit OFF and exact-set source switching still remove source ownership before requesting `Item_Equipped`, so they remain intra-execution operations. | CONFIRMED AS SOURCE IMPLEMENTATION; BUILD AND RUNTIME PENDING | `Script_FrameCollisionTest.cpp` source audit, 2026-08-23 | Build v0.16, repeat complete Normal/Quick/Dual fixtures, then interrupt a two-contact 2H Normal after its first RIGHT and immediately perform a complete new attack. The new first RIGHT must show a fresh budget and the second RIGHT must remain accepted. |

| EV-133 | FrameCollision v0.16 compiled and installed byte-for-byte, then completed a mixed interruption-boundary regression containing 38 independent marked executions: 25 complete 2H Normal double attacks, seven 2H Normal attacks interrupted after the first RIGHT, four complete 2H Quick double attacks, and two complete Dual alternating-source Normals. Every execution began with a fresh budget and every natural marker-owned retirement logged `RetiredMarkerExecutionCount: 1`. All 31 complete RIGHT/OFF/RIGHT or Dual alternating schedules retained their genuine later contact; none inherited an earlier occurrence count. The log also contains unmarked GetUpAttack/GetUpParade collision activity with zero reserved-marker callbacks. | CONFIRMED FOR TESTED PLAYER NORMAL/QUICK/DUAL INTERRUPTION MATRIX | source commit `286c7bf`; build/install SHA-256 `D7E0B425...6E02F`; archived v0.16 log + animation-author visual observations, 2026-08-23 | EV-131 is corrected for the tested weapon path. Natural marker-owned source retirement, not the controlled-callback rollback diagnostic, supplied the observed execution boundary in this session. The shared occurrence/source-set core is ready for isolated BOTH enablement; Fist/logical-source callback-boundary runtime coverage remains separate. |

| EV-134 | The v0.17 source candidate recognizes provisional `G3AB_COL_BOTH_TEST` as a distinct authored opcode whose desired exact source mask is RIGHT|LEFT. Exact-motion scanning and callback preflight require both equipped slot entities; missing either leaves the original Normal/Quick callback active. An accepted BOTH marker independently requests `Item_Attack` and clears the triggered list for both sources, retains only successfully activated weapon bits in the marker-owned window, completes Quick StatePosition bookkeeping once, and uses its own occurrence budget. Existing RIGHT, LEFT, OFF, duplicate, occurrence, exact-set transition, and v0.16 execution-boundary behavior is unchanged. | CONFIRMED AS NARROW SOURCE IMPLEMENTATION; BUILD AND RUNTIME PENDING | `Script_FrameCollisionTest.cpp` source audit, 2026-08-23 | Build v0.17. First validate one BOTH occurrence on marked Dual P0/P1 Normal fixtures, including two independent group requests/list clears and natural cleanup. Then repeat BOTH in one execution to validate coordinated rearm before testing mixed RIGHT/LEFT/BOTH/OFF transitions. |

| EV-135 | FrameCollision v0.17 compiled and installed byte-for-byte, then passed isolated single-occurrence BOTH activation. Sixteen marked P0 Dual Normal executions each accepted exactly one authored `G3AB_COL_BOTH_TEST`, required source mask 3, independently requested `Item_Attack` for RIGHT and LEFT, cleared both triggered lists, and published marker-owned mask 3. Gameplay consistently produced two contacts from the right -> left -> right fixture: the first right and middle left connected, while the final right did not re-hit without later rearm. Fifteen P1 positive-control executions retained the previously validated LEFT -> RIGHT -> LEFT marker schedule and could produce three contacts. | CONFIRMED FOR TESTED PLAYER DUAL NORMAL SINGLE-BOTH OCCURRENCE | source commit `931cc32`; build/install SHA-256 `F1E141BF...AB5A`; archived v0.17 log + animation-author visual observations, 2026-08-23 | Basic coordinated activation and independent list clearing pass. This does not yet validate repeated BOTH rearm, mixed exact-set transitions, or missing-required-slot native fallback. |

| EV-136 | Repeated BOTH rearm passed in the larger v0.17 Dual Normal session. Seventeen complete P0 executions each accepted `BOTH -> BOTH -> BOTH`, issuing six source collision requests and six triggered-list clears per attack; gameplay repeatedly damaged the same target on all three intended right -> left -> right contacts. Four additional P0 executions were interrupted after the second BOTH and reset both active weapon sources during Hit. Eleven complete P1 positive controls retained `LEFT -> RIGHT -> LEFT`; one P1 execution stopped after its first LEFT. Every one of the 33 accepted executions began with a fresh budget. Four unmarked GetUpAttack executions and one unmarked side Normal remained native, and no WhirlAttack motion occurred in the log. | CONFIRMED FOR TESTED PLAYER DUAL NORMAL REPEATED-BOTH AND INTERRUPTION MATRIX | archived v0.17 rearm log + animation-author visual observations, 2026-08-23 | Coordinated two-source rearm and same-target third contact pass. The accidental-action coverage does not validate Whirl ownership; missing-slot fallback and mixed RIGHT/LEFT/BOTH/OFF transitions remain separate. |

| EV-137 | v0.17 missing-source fallback passed with a marked P0 2H Normal motion requiring BOTH while only the right-hand Flamberge existed. All ten BOTH marker deliveries reported required mask 3, RIGHT resolved, LEFT unresolved, and `UNSUPPORTED SOURCE`; none issued a custom group request or triggered-list clear. The original callback remained active and produced ten native RIGHT 5 -> 7 activations near state time 0.25 plus ten phase-3 7 -> 5 resets. Gameplay produced the expected single contact from the delayed native window. | CONFIRMED FOR TESTED PLAYER 2H NORMAL INCOMPLETE-BOTH SOURCE SET | archived v0.17 missing-left fallback log + animation-author observation, 2026-08-23 | Exact-motion preflight is all-or-nothing for BOTH in the tested path: it neither suppresses native timing nor degrades to partial RIGHT ownership. Basic BOTH activation, rearm, interruption cleanup, and missing-source safety now pass; mixed exact-set transitions remain. |

| EV-138 | The controlled v0.17 mixed-set session produced 32 fresh player executions with no malformed sequence: 17 complete P0 `BOTH -> LEFT -> OFF -> BOTH`, 11 complete P1 `BOTH -> RIGHT -> OFF -> BOTH`, three interrupted P0 `BOTH -> LEFT`, and one interrupted P1 `BOTH -> RIGHT`. Each of the 28 complete executions accepted one OFF, rejected two replayed middle callbacks by authored budget, rejected the replayed final BOTH by same-update dedupe, and visibly retained all three intended contacts. P0 OFF closed mask 2/LEFT; P1 OFF closed mask 1/RIGHT; every OFF deactivated one source and cleared no list. | CONFIRMED FOR TESTED PLAYER DUAL NORMAL MIXED EXACT-SET MATRIX | archived controlled mixed-set log + animation-author visual observations, 2026-08-23 | RIGHT, LEFT, BOTH, OFF, exact-set retirement, inactive gap, later BOTH reactivation, occurrence protection, duplicate protection, and interruption prefixes coexist correctly in the tested Normal path. |
| EV-139 | The v0.17 multi-target mixed-set follow-up contained three complete P1 `BOTH -> RIGHT -> OFF -> BOTH` executions and one interrupted `BOTH -> RIGHT -> OFF` prefix. All callbacks followed the authored order; the three complete attacks produced the expected two budget rejections and one duplicate rejection each, while all four OFF events closed RIGHT without clearing. Complete attacks visibly damaged multiple opponents across their intended contacts. | CONFIRMED FOR TESTED PLAYER DUAL NORMAL MULTI-TARGET SETUP | archived multi-target mixed-set log + animation-author observations, 2026-08-23 | Mixed exact-set control preserves multi-target behavior; the interrupted post-OFF execution caused no later stale marker. |
| EV-140 | The v0.17 overlapping battle stress session reconstructed 39 fresh marked executions across three actors with distinct equipped-source addresses: PC_Hero had 24 executions (eight complete), AssWarrior_07 six (four complete), and Silvio nine (seven complete). All 19 complete schedules exactly matched their P0/P1 mixed-set sequence; all 20 interruptions were valid authored prefixes, with no malformed or cross-actor continuation. NPC attacks visibly connected and at least one produced all three contacts. Sixty-six reserved callbacks received during actual Dual Power motions were rejected at the Normal/Quick context gate; no Power or Quick marker was accepted. The log loaded/unloaded normally alongside native animal, Quick, side-attack, and other combat activity. | CONFIRMED FUNCTIONAL NPC/ACTOR-LOCAL STRESS EVIDENCE; PERFORMANCE COST NOT MEASURED | archived NPC battle stress log + animation-author observations, 2026-08-23 | Actor-local source resolution/budgets survived the tested overlapping battle without crash or observed contamination. Verbose prototype logging and absence of frame-time measurements prevent a claim that release overhead is negligible; benchmark a quiet build later. |

| EV-141 | The v0.17 Dual Quick mixed-set regression reconstructed 47 fresh Quick executions across all four tested variants: 19 P0 QuickAttackR/action 4, 13 P0 QuickAttackL/action 5, nine P1 QuickAttackL/action 5, and six P1 QuickAttackR/action 4. Forty-five completed the full sequence and two P0 QuickAttackR executions ended as valid prefixes after OFF. P0 accepted BOTH -> LEFT -> OFF -> BOTH; P1 accepted BOTH -> RIGHT -> OFF -> BOTH. Every first BOTH logged `ExecutionBudgetReset: 1` and Quick StatePosition 0 -> 1; later accepted source markers preserved 1 -> 1. No sequence was malformed and no delayed native 5 -> 7 followed cleanup. The author observed all three intended contacts. | CONFIRMED FOR TESTED PLAYER DUAL QUICK MIXED EXACT-SET MATRIX | archived v0.17 Dual Quick mixed-set log + animation-author visual observations, 2026-08-23 | The shared RIGHT/LEFT/BOTH/OFF source-set, replay-guard, execution-budget, cleanup, and Quick bookkeeping core passes both owned Normal and Quick families. This is the planned checkpoint before production marker-vocabulary discussion; Whirl and Power remain separate adapters. |
| EV-142 | During prolonged rapid-hit testing, some contacts no longer produced visible target stumbles even though the target's health continued to decrease. Separate genuine misses were observed when the attacker stood nearly inside the target and the weapon passed outside the useful collision geometry. | CONFIRMED AS A TEST-INTERPRETATION LIMITATION; EXACT RAGE RULE UNVERIFIED | animation-author observation during archived v0.17 Dual Quick session, 2026-08-23 | Do not use stumble animation alone as proof of damage or a miss. Health loss is stronger visual evidence in this setup. The exact rage/stumble-immunity trigger and New Balance configuration were not measured here; consider a future logger extension that observes damage/health change if per-contact attribution becomes necessary. |

| EV-143 | The equipped-slot marker API is frozen as `G3AB_COL_RIGHT`, `G3AB_COL_LEFT`, `G3AB_COL_BOTH`, and `G3AB_COL_OFF`. RIGHT/LEFT identify equipped slot entities, not animation-direction suffixes. The commands retain the validated exact-set/rearm/OFF semantics and remain independent of attack family. v0.18 replaces the four provisional `*_TEST` strings without aliases and otherwise leaves the v0.17 collision core unchanged. Body/unarmed/Fist/monster marker terminology remains open. | CONFIRMED DESIGN DECISION AND NARROW SOURCE IMPLEMENTATION; BUILD/RUNTIME PENDING | user approval + `Script_FrameCollisionTest.cpp` source audit, 2026-08-23 | Build v0.18, reauthor one Normal and one Quick fixture with final names, and run a compact regression before adding Whirl ownership. Historical evidence retains the marker strings actually used by earlier builds. |

| EV-144 | FrameCollision v0.18 compiled and installed byte-for-byte at SHA-256 `427A7CF4...AACCBC`, then completed 24 fresh final-name executions with no malformed sequence: 12 Normal (six P0, six P1) and 12 Quick (four P0 QuickAttackR, three P0 QuickAttackL, three P1 QuickAttackL, two P1 QuickAttackR). Every P0 execution accepted BOTH -> LEFT -> OFF -> BOTH; every P1 accepted BOTH -> RIGHT -> OFF -> BOTH. Totals were 48 accepted BOTH, 13 LEFT, 11 RIGHT, and 24 OFF; 48 interleaved replays were rejected by authored-occurrence budget and 24 final replays by same-update dedupe. All first markers reset their execution budget. All 12 Quick first markers changed StatePosition 0 -> 1 and all 24 later Quick source markers preserved 1 -> 1. Player collision transitions exactly matched 120 custom Item_Attack requests and 96 expected Item_Equipped requests (24 source switches, 24 OFF closures, 48 natural source resets), with no extra native player activation. All four final names appeared, no `*_TEST` name appeared, the log unloaded normally, and the author visually confirmed three intended contacts for every tested attack type. | CONFIRMED FOR FINAL EQUIPPED-SLOT VOCABULARY ACROSS TESTED PLAYER DUAL NORMAL/QUICK MATRIX | source commit `7550723`; build/install hashes + archived v0.18 final-marker log + animation-author observations, 2026-08-23 | v0.18 is the validated collision prototype. The equipped-slot marker API and shared Normal/Quick core are closed for this stage. Begin Whirl by mapping its native callback/action/phase path; do not infer support from filenames or broaden Power simultaneously. |
| EV-145 | Native Whirl logging separates two callback/action families despite their shared filename token. Full 2H/Staff Whirl ran as action 10 (`gEAction_WhirlAttack`), used RIGHT, activated near Hit state time 0.251–0.262, and normally reset in Recover near 0.803–0.809. Dual SimpleWhirl ran as action 6 (`gEAction_SimpleWhirl`): the logged P0 filename/runtime pose 2 case used LEFT and the P1 filename/runtime pose 1 case used RIGHT, activating near 0.253–0.260 and resetting near 0.431–0.438. Official SDK enums and pinned Jackydima source independently identify `OnAI_WhirlAttack` and `OnAI_SimpleWhirl` as separate callbacks. | CONFIRMED NATIVE CALLBACK/ACTION/SOURCE BASELINE FOR TESTED PLAYER 2H/STAFF/DUAL WHIRLS | archived v0.18 native-Whirl baseline + official SDK `GameEnum.h` + pinned Jackydima `Script_AttackCollision.cpp`, 2026-08-24 | Add full Whirl and SimpleWhirl as separate adapters and validations. Do not infer callback ownership from the animation filename's `WhirlAttack` token. |
| EV-146 | The v0.19 source candidate hooks only `OnAI_WhirlAttack`, requires exact action 10 plus Hit phase and exact-motion source-marker ownership, preserves the validated marker source/rearm/OFF core, and completes accepted source-marker StatePosition bookkeeping to 1. Dual SimpleWhirl/action 6 remains on the original callback. The adapter deliberately does not set `ResetOnUntouch`; authored repeated source markers rearm explicitly and OFF defines the inactive gap. | CONFIRMED NARROW SOURCE IMPLEMENTATION; 2H/STAFF CONTACT RUNTIME PASSED, STAFF CLEANUP EDGE OPEN | source commit `a78f231` + source audit + EV-147–EV-153 runtime evidence, 2026-08-24 | Preserve the adapter and validated contact core. Diagnose the rare Staff post-second-marker missing cleanup before declaring the full-Whirl boundary complete; keep SimpleWhirl separate. |
| EV-147 | FrameCollision v0.19 compiled and installed byte-for-byte at SHA-256 `76E1D284...5557EA` with validated v0.18 preserved at `427A7CF4...AACCBC`. In the native/no-New-Balance 2H full-Whirl test, 19 complete action-10 executions each accepted RIGHT -> OFF -> RIGHT, changed Whirl StatePosition 0 -> 1 then preserved 1 -> 1, rejected one replayed OFF by occurrence budget and one replayed final RIGHT by same-update dedupe, and followed the exact right-weapon sequence 5 -> 7 -> 5 -> 7 -> 5. No extra Whirl activation occurred. Both intended contacts were consistently observed and no premature second-swing hit was observed. The attempted interruption phase produced no incomplete marked Whirl execution. | CONFIRMED FOR TESTED PLAYER 2H FULL WHIRL WITHOUT NEW BALANCE | build/install hashes + archived native v0.19 log + animation-author observations, 2026-08-24 | Full-Whirl callback ownership, explicit rearm, OFF gap, bookkeeping, replay guards, and natural cleanup pass the native 2H fixture. Repeat the same controlled fixture with Staff before adding SimpleWhirl. |
| EV-148 | With New Balance enabled but its separate `Script_AttackCollision.dll` absent, v0.19 produced nine action-10 marked 2H Whirls: seven complete RIGHT -> OFF -> RIGHT executions and two valid interruptions after the first RIGHT. Complete executions preserved the exact 5 -> 7 -> 5 -> 7 -> 5 sequence and StatePosition bookkeeping; interrupted executions reset 7 -> 5 before OFF and delivered no stale later marker. No extra Whirl activation appeared. New Balance changed Whirl state-time pacing (complete markers approximately 0.18–0.19, 0.44–0.45, 0.67–0.68 versus native approximately 0.16–0.17, 0.40–0.41, 0.60–0.61) and restored post-Recover block behavior, but collision ownership remained correct. One incidental marked Normal execution and unrelated knockdown/dodge/body events are outside this Whirl count. | CONFIRMED COMPATIBILITY FOR TESTED NEW-BALANCE CONFIGURATION WITHOUT ATTACKCOLLISION DLL | archived New Balance v0.19 log + animation-author observations, 2026-08-24 | Keep this isolated New Balance result distinct from the later combined-DLL result recorded in EV-150. |
| EV-149 | Production compatibility must not weaken authored marker timing merely to preserve Jackydima's separate collision DLL. If direct hook coexistence cannot retain one authoritative collision owner, evaluate coordination with Jackydima or a documented replacement/integration package rather than running two competing Whirl collision systems. | CONFIRMED RELEASE DESIGN CONSTRAINT; PACKAGING DECISION OPEN | animation-author requirement and permission context, 2026-08-24 | The current marked 2H coexistence fixture passes in EV-150. Keep the broader packaging decision open until callback-family coverage, alternate load/timing conditions, and production integration are sufficiently complete. |

| EV-150 | With New Balance, current `Script_AttackCollision.dll` built from commit `5f046b0`, its original `attacks.ini` (`ActiveWhirlAttackStartTime=0.25`), and FrameCollision v0.19 loaded together, the marked 2H Whirl remained frame-controlled. Ten complete executions accepted RIGHT -> OFF -> RIGHT and followed exact right-source 5 -> 7 -> 5 -> 7 -> 5 behavior; two interrupted executions accepted the first RIGHT and safely reset 7 -> 5. First RIGHT arrived at state time 0.112–0.187, before the third-party 0.25 threshold, changed Whirl StatePosition 0 -> 1, and no extra/timer-owned 5 -> 7 activation appeared. Expected replay callbacks were rejected without mutation. Both intended contacts were visually confirmed. | CONFIRMED COMPATIBILITY FOR TESTED MARKED PLAYER 2H WHIRL INSTALLATION/LOAD ORDER | archived combined compatibility log + animation-author observations + current Jackydima source/build hash `A8C4BAC8...A79D`, 2026-08-24 | The current combined setup is compatible for this fixture. Do not generalize to arbitrary DLL load order, unmarked attacks, or markers occurring after the third-party threshold; retest if packaging, filenames/load order, or timing changes. Staff remains the next native-family fixture. |

| EV-151 | Native v0.19 Staff full-Whirl contact ownership passed the marked RIGHT -> OFF -> RIGHT fixture. Eight complete executions accepted the authored schedule with correct StatePosition bookkeeping, replay rejection, and visually confirmed first/second contacts without observed premature second-window damage. Seven naturally reset 7 -> 5 in Recover. In the first execution, however, no final natural reset/retirement followed the second RIGHT; about 4.27 seconds later the next Whirl began while the right source was still group 7 and the previous marker-owned mask was still 1. | CONFIRMED STAFF CONTACT/REARM/OFF BEHAVIOR; ONE REAL POST-HIT CLEANUP ANOMALY | archived initial Staff v0.19 log + animation-author observations, 2026-08-24 | Keep the full-Whirl adapter, but do not declare its cleanup boundary closed. Reproduce the skipped-Recover condition before changing architecture. |
| EV-152 | Controlled cleanup follow-ups produced 12 additional complete Staff Whirls: one idle, one first chained execution, and ten broad-control executions. All 12 naturally reset and retired after the second RIGHT. Together with EV-151 this is 20 complete Staff Whirls with 19 natural final cleanups and one missing cleanup. A second chained attempt accepted RIGHT -> OFF, then action changed to 0 before the late RIGHT; the remaining replay callbacks were rejected and collision was already safely closed by OFF. The broad control also completed nine clean 2H Whirls; incidental Finishing/Normal actions were excluded. | CONFIRMED RARE STAFF CLEANUP EDGE; NORMAL AND PRE-LATE-MARKER INTERRUPTION PATHS PASS | three archived v0.19 cleanup-boundary logs + initial Staff log, 2026-08-24 | Next isolate the suspected block-duration/Recover-skip boundary. Do not add an ordinary second authored OFF: Gothic's replayed marker-name sequence can consume the final OFF occurrence at the late RIGHT and close the intended second window immediately. |
| EV-153 | In the tested native player 2H and Staff full-Whirl executions, Hit began without the corresponding Raise animation playing. This matches the animation-author's direct observation in both families. | CONFIRMED NATIVE RUNTIME OBSERVATION FOR TESTED 2H/STAFF WHIRLS | v0.18/v0.19 Whirl logs + animation-author observation, 2026-08-24 | When Raise generalization resumes, include 2H and Staff full Whirl alongside Normal and Quick; keep Raise work separate from current collision cleanup diagnosis. |

## 3. Evidence Promotion Rule

A finding may move upward only when a new source or controlled test justifies it.

Design decisions never become engine facts merely because they are implemented.

## 4. Regression Rule

If a later code revision contradicts an earlier confirmed result:

1. reproduce using the known-good baseline if available;
2. determine whether the engine/build changed or the new code regressed;
3. update this ledger with the new evidence;
4. do not silently delete the old result.
