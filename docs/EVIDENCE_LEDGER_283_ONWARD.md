# Gothic 3 Animation Behaviors — Evidence Ledger: EV-283 Onward

**Status:** Canonical continuation after `EVIDENCE_LEDGER_280_ONWARD.md` EV-282  
**Date:** 2026-09-15

**ID range:** EV-283 onward

## Purpose

Continue the single global monotonic evidence namespace after EV-282. This split is storage/retrieval only; it does not create a new evidence namespace.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-283 | The cross-family raw55 two-FIST runtime proves repeated authored FIST delivery and exposes where the current family-specific diagnostic scaffolding is insufficient to determine final repeated-hit semantics. **Quick:** repeated authored FIST is positively causal. In factual Quick C1=3 the first FIST owns the proven raw55 opening/rearm path and native damage to `PC_Hero`; the second authored FIST is delivered later in the same C1 while RIGHT remains PhysicalFist/raw55 group7, `CORE RAW55_QUICK_REPEAT_FIST_REARM_PROBE` records `DecisionFistCount=2`, `ClearTriggeredList=1`, `REPEAT_FIST_REARM=1`, and a second native `ONDAMAGE Target=PC_Hero` follows in that same C1. Other Quick executions, including C1=1, show a second marker/rearm opportunity without a guaranteed second damage event, confirming that authored rearm creates an opportunity and does not replace geometric contact. **Normal:** multiple executions reveal a pre-state ordering boundary not covered by the current one-shot Normal probe. In examples including C1=18, 99 and 101, the first FIST is transported while `StatePosition=0`, before the Normal early-suppression proof exists; the current Normal activation probe therefore does nothing at that first marker. The later FIST at `StatePosition=1` becomes the first and only probe-owned `5 -> 7` activation and can produce native damage. This does not prove Normal is one-hit-only; it proves the current diagnostic policy cannot test the first pre-state occurrence or a later repeat after opening. **Power:** factual Power executions transport a later second FIST in the same C1 after the first SP1 FIST has already opened RIGHT raw55 and produced native damage, but the diagnostic Power proof is deliberately one-shot (`activationUsed`), so no second activation/rearm intervention occurs. The absence of a second hit is therefore not evidence that Power cannot double-hit. **Sprint:** C1=87 and C1=109 repeat a more specific ownership transition. The first FIST is delivered while the same C1 is factual `Action=9 / Family=SPRINT`, opens exact RIGHT raw55 `5 -> 7` with `ClearTriggeredList=0`, and native damage follows. Immediately after first contact the same C1 / same PowerAttack-named motion becomes factual `Action=2 / Family=POWER`; the second authored FIST is then delivered under Action2 while RIGHT remains group7. Neither the already-used Sprint one-shot proof nor the separate Power proof owns that second occurrence, so no repeat rearm occurs and only the later native `7 -> 5` cleanup closes the obligation. This proves that repeated-marker ownership cannot be modeled as a one-shot per-family probe permission and that the Action9->Action2 transition must not be mistaken for a new C1/attack execution. Across the exercised paths Gothic cleanup remains healthy. | CONFIRMED CROSS-FAMILY TWO-FIST TRANSPORT + QUICK SAME-C1 SECOND REARM/SECOND-DAMAGE PASS; NORMAL PRE-STATE FIRST-MARKER GAP EXPOSED; POWER AND SPRINT SECOND-MARKER RESULTS ARE INCONCLUSIVE BECAUSE CURRENT ONE-SHOT DIAGNOSTIC PROOFS DO NOT ATTEMPT REARM; SPRINT SECOND MARKER CAN ARRIVE AFTER SAME-C1 ACTION9->ACTION2 TRANSITION. | Canonical runtime `research/raw/2026.09.15_troll_raw55_double_fist_activation.log`, upload commit `be2db3e8f2f8f1aa09ed02cf8aad918ae0e6a1a4`, SHA256 `3771B3A9601D5CF3A715B6B15DF39DA5B55B0881CF8A127D1E9CFE436E898E43`. POP-07 package `research/derived/2026.09.15_troll_raw55_double_fist_activation_large_log/`. Runtime uses the diagnostic family mechanisms already closed through EV-282. EV-270 and EV-273 remain the governing Quick precedent: the temporary one-shot probe rule is not final authoring semantics, and each legitimate Quick FIST occurrence can be its own proven rearm opportunity. User runtime observation on 2026-09-15 independently confirms that Quick attacks with the two authored markers visibly damaged the player twice. | Do **not** promote any family probe's `activationUsed` one-shot bookkeeping into permanent raw55 architecture. Permanent design must eventually express authored FIST occurrences within the factual C1/source execution, not merely a one-shot permission tied to whichever family label is current at the marker. Before permanent architecture is frozen, close the two newly exposed causal boundaries separately: (1) Normal pre-StatePosition authored-FIST ownership, using EV-269 as the Quick precedent but without assuming Normal needs the same contact primitive; and (2) repeated-FIST behavior for the non-Quick raw55 path, including what exact operation is required when the source is already group7 and, for Sprint-origin executions, the same C1 has transitioned from Action9 to Action2. Do not infer `ClearTriggeredList` for Normal/Power/Sprint merely from Quick; test the smallest repeated-contact intervention causally. Preserve native callback/state progression and native cleanup. |
| EV-284 | The current New Balance 0.7.0 source is publicly available in `Jackydima/gothic3sdk/scripts/Script_NewBalance` and passes the source-level coexistence preflight against the collision/marker surfaces currently owned by Gothic3_Animation_Behaviors. The New Balance resource identifies `FILEVERSION/PRODUCTVERSION 0.7.0.0`, and repository `master` is current through `a9f736603bb42c40b6e1556619051b57f9f525b6` dated 2026-09-14. The active Fist-specific 0.7.0 change is animation-speed policy in `FunctionHook.cpp`: `GetAnimationSpeedModifier` recognizes humanoid Fist mode and returns `0.7f` for factual `gEAction_Attack` and `0.8f` for `gEAction_PowerAttack`; commit `1d09f8d177f7d18789dd55433be69703564bb0be` raised these from `0.54f` and `0.6f`. New Balance installs that policy at `RVA_ScriptGame(0x42a0)`. Its former Fist friendly/current-target byte patch at `Script_Game +0xAA5E6..+0xAA5EB` is present only as commented-out code in current `CodePatch.cpp` and is not active. New Balance does use `TouchDamage.ClearTriggeredList()` in `FixDualOneHanded`, but that intervention is explicitly gated to dual-1H ownership and inserted at `RVA_ScriptGame(0x482e7)`; it is not the PhysicalFist/raw55 path. New Balance also inserts combat-move scaling at `RVA_Game(0x16b8a9)`, while this project owns the enclosing `AICombatMoveInstr` entry transport at `RVA_Game(0x1696E0)` and calls the original function; these are distinct hook sites and can compose. Targeted source checks identify no New Balance ownership of this project's `_AI_Attack`, `_AI_PowerAttack`, `_AI_QuickAttack`, `Engine +0x225660 SetCollisionGroup`, or `Game +0x60850` marker transport sites. | CONFIRMED NEW BALANCE 0.7.0 SOURCE-LEVEL COMPATIBILITY PREFLIGHT: NO DIRECT HOOK/ADDRESS OR RESPONSIBILITY COLLISION IDENTIFIED WITH CURRENT RAW55/MARKER TRANSPORT. ACTIVE FIST CHANGE IS ANIMATION SPEED, NOT COLLISION-WINDOW OWNERSHIP. | Current source `Jackydima/gothic3sdk`, `scripts/Script_NewBalance`, master `a9f736603bb42c40b6e1556619051b57f9f525b6`; version authority `Script_NewBalance.rc` = `0.7.0.0`; Fist speed commit `1d09f8d177f7d18789dd55433be69703564bb0be`; current `FunctionHook.cpp`, `CodePatch.cpp`, `CallHook.cpp` reviewed against current `prototypes/Script_FrameCollisionTest/EngineBridge.cpp`. | Treat New Balance's Fist speed policy and combat-move scaling as external gameplay behavior that this mod must preserve rather than override. Source-level compatibility is PASS, but final product compatibility still requires one runtime coexistence test with New Balance 0.7.0 loaded because faster animation/movement can change practical contact timing even without a hook collision. The coexistence runtime should verify marker delivery, raw55 opening/rearm, native damage, and native cleanup while New Balance is active; do not require a binary reverse-engineering audit unless runtime or source later contradicts this preflight. |
| EV-285 | `Jackydima/gothic3sdk/scripts/Script_AttackCollision` is **not** a compatible simultaneous collision authority for Gothic3_Animation_Behaviors. It is a separate optional DLL target from `Script_NewBalance`, with no New Balance link/load dependency, but it hooks the same AI callback functions used by this project's marker ownership: `OnAI_Attack`, `OnAI_PowerAttack`, `OnAI_QuickAttack`, `OnAI_PierceAttack`, `OnAI_SimpleWhirl`, and `OnAI_WhirlAttack` (plus `OnAI_GetUpAttack`). Its callbacks do not delegate to the original callback; instead they implement their own fixed-`StateTime` collision policy, directly call `SetCollisionGroup(Item_Attack)`, call `TouchDamage.ClearTriggeredList()`, and write `StatePosition`. Defaults are timer-based (`Attack=.25`, `GetUp=.1`, `Pierce=.8`, `Power=.1`, dual-Power repeat clear `1.0`, `Quick=.1`, `SimpleWhirl=.25`, `Whirl=.25`). Its hand/source rules are hard-coded by pose/loadout rather than authored markers: dual Normal P0 opens LEFT; dual Pierce/SimpleWhirl P1 opens LEFT; Quick with dual-1H **or Torch+1H** at P1 opens LEFT; Power opens RIGHT plus LEFT for dual-1H and later clears both again; Whirl opens RIGHT and sets `ResetOnUntouch`. By contrast this project gives the animation author exact `RIGHT/LEFT/BOTH/OFF` source control, repeated marker rearm, exact-source lifecycle bookkeeping, action-family ownership, and supports additional proven scope such as Hack. Because both DLLs patch the same callback entrypoints and both claim collision-window/rearm/StatePosition responsibility, simultaneous installation is load-order-dependent and semantically contradictory even if a particular hook chain happens to run. In particular AttackCollision's Torch+1H Quick rule may activate the left-hand torch as an attack source, directly contradicting this project's exact-slot marker authority. | CONFIRMED STATIC INCOMPATIBILITY / MUTUALLY EXCLUSIVE COLLISION CONTROLLERS. `Script_NewBalance` 0.7.0 REMAINS SOURCE-LEVEL COMPATIBLE; `Script_AttackCollision.dll` MUST NOT BE LOADED ALONGSIDE THIS PROJECT'S MARKER COLLISION SYSTEM. | Current source `Jackydima/gothic3sdk`, `scripts/Script_AttackCollision/Script_AttackCollision.cpp`, latest path commit `da61a791a97704ecebf166768c30564b6332d82d` dated 2026-08-20; separate-library evidence from `scripts/Script_AttackCollision/CMakeLists.txt`, `scripts/Script_NewBalance/CMakeLists.txt`, and root `scripts/CMakeLists.txt`; marker semantics from this project's `ANIMATION_RULES.md` and `FrameCollisionMarkers.cpp`. | Product compatibility wording must distinguish **New Balance** from **AttackCollision**. Supported combination is `Script_NewBalance + Gothic3_Animation_Behaviors`, with `Script_AttackCollision.dll` absent/disabled. Do not attempt to make two collision controllers coexist through load order. Before release, add a clear install/runtime compatibility guard or warning for `Script_AttackCollision.dll`; `attacks.ini` is AttackCollision timing configuration and has no role in this project's marker semantics. Runtime New Balance coexistence validation should therefore be performed with New Balance 0.7.0 active and AttackCollision absent. |
