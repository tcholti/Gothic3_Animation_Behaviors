# Between Chats

**Purpose:** Exact short-lived continuation bridge. Replace rather than accumulate chronology.

**Updated:** 2026-09-07

## Current bridge — Transaction 1 COMPLETE; Transaction 2 NEXT

Repository: `tcholti/Gothic3_Animation_Behaviors`  
Active branch: `docs/collision-source-evidence`  
Stable branch: `main`

Fresh Chat / Work must read `docs/SESSION_ENTRYPOINT.md` first, then this file, then `docs/WORK_IMPLEMENTATION_PROTOCOL.md` only when a bounded Work implementation is actually frozen.

No Work implementation task is currently frozen.

---

## Durable checkpoint from 2026-09-07

Published Transaction 1 commit:

```text
239c076002c0dc47bb1464cfb995a68490432ae6
Close September evidence transactions
```

Transaction 1 repaired evidence/provenance integrity without changing collision implementation:

```text
EV-241  Power equipped marker/source/lifecycle expansion closure
EV-242  Pierce equipped marker/source/lifecycle closure
        + bounded Pierce-specific New Balance + Jackydima compatibility control
EV-243  SimpleWhirl StatePosition-2 causal hypothesis rejected
        + StatePosition-1 restored/pass
        + bounded target-semantics closure
EV-244  Hack actual 2H/Staff runtime routing/source/marker closure
        while EV-216 remains callback identity only
```

Procedure maintenance:

```text
PROJECT_OPERATING_PROCEDURES.md -> v1.11
POP-06 now requires same-investigation evidence closure before advancing
POP-11 requires missed evidence closure to be repaired during Chat recovery/transition
```

Evidence storage maintenance:

```text
50 processed artifacts moved byte-identically from research/raw to research/archive
49 dated September 2-7 runtime logs
+ ge3log10.log historical crash provenance

research/raw now contains only Keep.txt
```

Canonical migration routing is recorded in `docs/EVIDENCE_PATH_MIGRATIONS.md`.

---

## Closed / protected technical boundary

```text
collision architecture verification                 COMPLETE — EV-206–EV-215
Power equipped marker/source validation             CLOSED/PASS — EV-241
Pierce equipped marker/source validation            CLOSED/PASS — EV-242
SimpleWhirl current marker/semantic scope           CLOSED/PASS — EV-217–EV-220, EV-243
Hack tested 2H/Staff routing/source/marker scope    CLOSED/PASS — EV-216, EV-244
human Fist N4 callback suppression                  CLOSED — unnecessary
human Fist N5 ClearTriggeredList                    CLOSED — unnecessary
human Fist N6 native timing gate                    CLOSED/PASS
human Fist authored FIST_OFF                        CLOSED — unnecessary
human Fist interruption handling                    CLOSED/PASS for tested Normal/Power cases
production human raw-8 FIST                         CLOSED/PASS — EV-221–EV-240
```

Do not reopen these findings without concrete contradictory evidence.

Production human-Fist acceptance is now archived at:

```text
research/archive/2026-09-07_fist_production_frame12_normal_power_acceptance.log
```

Final tested visual result remains 6/6:

```text
first swing  = NO damage
second swing = YES damage
```

---

## Current exact next responsibility — Transaction 2

> Consolidate the technical authorities so they describe the proven current collision state and the intended next roadmap correctly.

This is an **authority/documentation consolidation transaction**, not a new collision implementation or runtime experiment.

Transaction 2 should cover only the stale/current technical owners needed to make these points durable and mutually consistent:

```text
1. final production human raw-8 Fist architecture and exclusions;
2. raw55 / gEUseType_PhysicalFist remains a separate bounded future discovery stage,
   not a generalization of the proven human raw-8 mechanism;
3. AttackContinuationProtection returns to its correct later roadmap responsibility,
   including the requirement that any future solution preserve New Balance compatibility;
4. stale collision / animation / source-hook authority text is corrected to the proven
   Power / Pierce / SimpleWhirl / Hack / Fist state;
5. evidence references use the promoted EV-241–EV-244 and current archive routes where appropriate.
```

Preserve during Transaction 2:

```text
no C++ behavior change
no build or deployment
no new marker vocabulary
no new Fist experiment
no raw55 implementation/probe
no AttackContinuationProtection implementation
no equipped source/lifecycle redesign
no CAM change
```

The aim is to make the technical authorities accurately describe what is already proven and to place the still-future responsibilities correctly.

After Transaction 2, re-read the resulting current authorities before freezing the next runtime/implementation step. Do not blindly resume an older NEXT pointer if the consolidation changes routing.

---

## Tomorrow bootstrap

A fresh Chat should:

```text
read docs/SESSION_ENTRYPOINT.md
-> read this file
-> confirm remote branch HEAD is at or after the continuity checkpoint
-> retrieve only the technical authorities needed for Transaction 2
-> perform Transaction 2
```

Do not reconstruct September raw evidence from scratch. Use `docs/EVIDENCE_INDEX.md`, EV-221–EV-244 and `docs/EVIDENCE_PATH_MIGRATIONS.md` when exact proof/provenance matters.
