# Between Chats

**Purpose:** Small transient bridge between Normal Chat and Work.
**Rule:** Keep this file short and overwrite the current handoff; do not accumulate history here.

## Work result — Step B6-C3 factual empty-Primary successor stack diagnostic

**Date:** 2026-08-28
**Branch:** `docs/collision-source-evidence`
**Implementation commit:** `9f99d3d43a66321a9a0ddbc1df882d6daee6dddd`

### Changed

- Refined the existing player/type-0 empty-Primary PlayMotion probe so its pre-original gate is only an available Primary snapshot with no motion instance.
- Removed the previous Normal attack-family predicate without replacing it with another semantic or cause gate.
- Renamed the helper and record to factual `EMPTY-PRIMARY SUCCESSOR STACK` terminology.
- Updated the runtime banner with `UNGATED EMPTY-PRIMARY SUCCESSOR`.

### Protected behavior and source audit

- The existing 16-frame capacity and factual context/stack capture are unchanged.
- Original PlayMotion remains called exactly once per existing path with unchanged arguments and ordering.
- The post-original record still requires an available, installed successor Primary instance and reports its name factually.
- Direct PlayMotion replacement, PrimaryFirst, StopMotion, StartRecover and B4/B5 cleanup diagnostics are unchanged.
- No new hook, production behavior, state, timer, polling, scan, cache, classifier, collision-group gate, successor-name gate or guessed layout was added.
- Collision, marker, callback and bookkeeping behavior is untouched.
- Only the three allowed source files changed; `git diff --check` passed.

### Still required outside Work

- Independent diff-against-contract review.
- Build/load verification, then repeat the short controlled B6-C legitimate interruption fixture and inspect factual Stumble/KnockDown successor stacks.
- No build or Gothic 3 run was performed in Work, as required.
