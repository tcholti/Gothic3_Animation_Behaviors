# Between Chats

**Purpose:** Short-lived exact continuation pointer. Replace, do not accumulate.  
**Updated:** 2026-09-20

> After abrupt/max-context recovery, start at root `README.md` and apply POP-11 before trusting this bridge.

## Current state

Repository: `tcholti/Gothic3_Animation_Behaviors`  
Branch: `docs/collision-source-evidence`

Phase 4 raw8 FIST authoring-semantics research remains active.  
Latest completed evidence: **EV-353**.  
Active Work task: **NONE**.

Parent review:
`docs/work/active/COLLISION_RAW8_FIST_AUTHORING_SEMANTICS_REVIEW.md`

Closed causal probe:
`docs/archive/investigations/COLLISION_RAW8_PERSISTENT_OPPORTUNITY_TOKEN_CAUSAL_PROBE.md`

## EV-353 closure

Diagnostic implementation `015a3ef90bf135cf502ac6507fb68f7c8c962800` passed runtime causal validation.

Marked accounting:
- 81 FIST opens;
- 59 native contact consumptions;
- 21 C1 lifecycle closes;
- 1 same-C1 later-FIST supersession;
- 18 contacts occurred only after one or more miss-rearms;
- zero observed cross-C1 old-opportunity contacts.

Tested controls include Gargoyle Power, marked human Normal/Power, native unmarked human fallback, target-directed group combat, ordinary finalization and reaction-generation replacement.

Durable semantic:

```text
FIST -> one pending target-directed opportunity
miss -> preserve + restore native one-shot eligibility
first exact Game+0x16E348 raw8 contact dispatch -> consume
later FIST -> reopen one, never stack
same-C1 Action/family transport -> preserve
exact C1 end/replacement -> close
unmarked raw8 -> native
```

Raw8 remains separate from equipped/raw55. No target list, FIST_OFF, custom contact or custom damage.

## Next gate

Run:

```text
python tools/knowledge/validate_knowledge_state.py
```

Require `Knowledge-state validation PASS`.

After PASS, Normal Chat may freeze the smallest **permanent raw8 architecture** from the proven causal model. Do not implement production code before that freeze.

`research/raw/` should contain only `Keep.txt`.
