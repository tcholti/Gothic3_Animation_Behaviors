# Between Chats

**Purpose:** Exact short-lived continuation bridge. Replace rather than accumulate chronology.  
**Updated:** 2026-09-19

> After an abrupt/max-context/unusable Chat, start at root `README.md`; this bridge is only a clue until POP-11 recovery verifies it.

## Current bridge

Repository: `tcholti/Gothic3_Animation_Behaviors`  
Branch: `docs/collision-source-evidence`

Current gate: **equipped Sprint permanent extraction IMPLEMENTED + independent source-review PASS; build/runtime acceptance pending.** Phase 4 remains held.

Active contract:

`docs/work/active/COLLISION_EQUIPPED_SPRINT_PRODUCTION_PROMOTION.md`

Reviewed implementation:

`218c7ab73815a9afc46fc3ea5ac7246998bc2b1b`

Source-review result:
- permanent `EquippedSprintCollision.cpp/.h` replaces the temporary probe;
- it is compiled through shared `FRAME_COLLISION_BEHAVIOR_SOURCES` into behavior-only and diagnostic twins;
- EngineBridge remains hook transport;
- diagnostics only observe behind `FRAME_COLLISION_DIAGNOSTICS`;
- true Power cannot create Sprint origin;
- missing required sources remain fail-closed;
- Action9 -> same-C1 Action2 continuation remains exact-identity-only;
- generic RIGHT/LEFT/BOTH/OFF mechanics remain owned by `FrameCollisionMarkers`;
- raw8/raw55/lifecycle/damage ownership were not changed.

No build was performed by Work, so promotion is **not yet runtime accepted**.

Next session, in order:
1. pull current branch and run knowledge validator;
2. build both `Script_FrameCollisionBehaviorTest` and `Script_FrameCollisionTest`;
3. verify diagnostics-free behavior twin startup/load;
4. deploy diagnostic twin and rerun compact EV-322 RIGHT->OFF, EV-323 BOTH-missing-LEFT, EV-324 true-Power controls;
5. deploy behavior-only twin and smoke the known Goblin Sprint fixture;
6. only after all PASS close/archive the promotion contract and update permanent production support.

Latest runtime evidence remains EV-324. `research/raw/` should contain only `Keep.txt`.

Use `COLLISION_REFERENCE.md` for established facts, `DESIGN.md` for architecture, and `EVIDENCE_INDEX.md` for provenance.
