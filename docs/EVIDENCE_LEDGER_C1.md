# Gothic 3 Animation Behaviors — Evidence Ledger: C1 Continuation

**Status:** Canonical continuation of `EVIDENCE_LEDGER_STEP_B.md`  
**Date:** 2026-08-29  
**ID range:** EV-190 onward

## Purpose

This file preserves the C1-era collision-lifecycle evidence after Step B closed at EV-189. Read `EVIDENCE_LEDGER.md` for EV-001 through EV-157, `EVIDENCE_LEDGER_STEP_B.md` for EV-158 through EV-189, and this file for EV-190 onward.

The evidence namespace remains one global monotonic EV sequence. This file split is only a storage/retrieval boundary; it does not create a new evidence namespace.

The same promotion rule applies: design choices do not become engine facts by repetition, source/prototype facts are labeled separately from engine/runtime facts, and hypotheses remain explicitly qualified.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-190 | B8 generalized the tested held-Use2 stale-collision failure beyond full Whirl. Player Quick attacks with Dual (1H+1H), plain 1H, and Shield+1H were observed using the same `Script_Game +0x633F1` `PSRoutine::FullStop()` path (observed return `+0x633F7`) and could enter successor Ambient/Parade while an equipped weapon remained `Item_Attack(7)`. | CONFIRMED FOR TESTED PLAYER QUICK CONFIGURATIONS | raw `research/raw/2026-08-28_b8_player_dual_quick_state_stack_abandonment.log`, commit `3c8484f3e6a123e6a5c5901318bd9c444cc0107b`; B8/B9 result record commit `c94fd8ca4187ef0dc973e9fbe90c91c5e33cf752`, 2026-08-28 | The known failure class is not a Whirl-only or Dual-only animation defect. Continue with one execution-level cleanup invariant and do not repair at the family-specific held-Use2 call site. |
| EV-191 | B9 established the tested cleanup/finalization ordering around `gCScriptRoutine_PS::AISetState`. Armed bad held-Use2 full-Whirl cases ordered as `AIFullStop -> CombatMove fullStop -> AISetState` with no native collision cleanup in between; `AISetState` was entered while the old attack context and group-7 weapon were still present. Clean full Whirl/Normal/Quick completion performed native `7 -> 5` cleanup before the later `AISetState(...PS_Melee_Loop)`. Tested legitimate armed Normal/Quick reactions performed FullStop and then the established reaction cleanup before reaction ownership continued, with no intervening AISetState before that cleanup. The run also exposed another legitimate-reaction AIFullStop caller at `Script_Game +0x2B8CB`; the Pierce Raise subtest did not directly reproduce destructive abandonment inside `PS_Melee_PierceAttack`. | CONFIRMED FOR TESTED PLAYER BAD/CLEAN/REACTION ORDERING; PIERCE DESTRUCTIVE-SKIP SUBCASE NOT PROVEN | B9 implementation `55e268901aaf06758cb5d88085c865457684992c`; raw `research/raw/2026-08-28_b9_player_aisetstate_cleanup_ordering.log`, commit `ae0a78787cb811f9de551997505ea4bd14370846`; interpreted result commit `c94fd8ca4187ef0dc973e9fbe90c91c5e33cf752`, 2026-08-28 | Destructive AISetState is a strong tested post-opportunity checkpoint for the known abandonment class only when gated by exact execution/source obligation state. AISetState is generic and must not become unconditional cleanup. One reaction FullStop call site is not universal. |
| EV-192 | The C1 shadow prototype implements an actor-generic monotonic generation plus exact equipped-source obligation model. A candidate begins only on a non-FullStop CombatMove invocation with valid args/SPU, snapshots exact RIGHT/LEFT equipped entities, persists only when the original CombatMove reports incomplete, counts successful `Item_Attack` results including `7 -> 7` as source obligations, fulfills an obligation when the resulting group leaves `Item_Attack`, and evaluates the captured generation after original AISetState. `WOULD_REPAIR` is log-only; C1 does not physically call SetCollisionGroup, retire marker state, alter StatePosition, suppress callbacks, poll, scan, or classify by family/action/state/input. | CONFIRMED PROTOTYPE SOURCE BEHAVIOR; NOT BY ITSELF ENGINE EVIDENCE | implementation `3778517f421d07e940c620745bc7ccdf0da54313`; independent diff-against-contract review PASS `f04c3e18f4129e0ee7727a3f266d6fc55fb13a8c`, 2026-08-28 | C1 provides a shadow-only test vehicle for the execution/source cleanup invariant while preserving existing physical collision and marker semantics. Runtime evidence is required before promoting any classification or boundary. |
| EV-193 | The C1 core runtime matrix validated the shadow consequence-based classification for the main tested player cases. Eight actual `WOULD_REPAIR` event outcomes were accounted for by known armed bad held-Use2 abandonments (1 full Whirl, 7 Dual Quick). Clean ordinary completions, tested pre-activation bad skips, and tested legitimate reaction interruptions did not become repair candidates. In the inherited-stale control, a bad Whirl left the weapon at group 7; the next Normal execution legitimately requested `7 -> 7`; C1 attributed that request to the new generation; later native `7 -> 5` cleanup fulfilled that new generation. No `OVERLAP_OUTSTANDING`, `FINALIZATION_GENERATION_CHANGED`, or `CANDIDATE_GENERATION_CHANGED` invariant was reported in the extract. | CONFIRMED FOR TESTED PLAYER C1 CORE MATRIX | raw `research/raw/2026-08-28_c1_shadow_core_lifecycle_matrix.log`, commit `a3c41c829a0e7d083ccfc657eafc285f68b60d4b`; derived `research/archive/2026-08-28_c1_shadow_core_lifecycle_matrix_connector_extract.txt`, commit `e5d8da1323b3c43c52ce0f58ea010081e00a364f`, source SHA256 `72D3E6DFBA8F8B4E6325F2FCF1B9D0F87753D33BFC34B60DF925A0615D3DDDB6`, 2026-08-29 | The per-source obligation model and destructive-finalization classification are strong enough to preserve as the C1 core. The remaining problem is acquisition/lifetime correlation, not a need for family/cause-specific cleanup logic. |
| EV-194 | The same C1 matrix exposed 11 `UNOWNED_PLAYER_OFFENSE_REQUEST` warnings around knockdown/GetUp handling. Static inspection of the tested GetUpAttack Script_Game region shows that the weapon can be set to `Item_Attack(7)` around `Script_Game +0x41CA6`, before the later CombatMove call at `+0x41D5A`, with ordinary GetUp cleanup later at `+0x41E10`. Therefore a legitimate collision-owning attack ScriptFunction can begin its offensive work before the C1 generation boundary that was then tied to new CombatMove initial invocation. | CONFIRMED FOR TESTED C1 RUNTIME WARNINGS + TESTED STATIC GETUP CONTROL FLOW | C1 raw/extract from EV-193; GetUp static/control-flow finding recorded in commit `27d7768cfa7a8d0d06cc178abe08505e9a15ee8e`, 2026-08-29 | CombatMove is a strong inner asynchronous-instruction boundary but not the universal outer collision-owning execution start. Do not solve the gap with a GetUp/action-30 special case or by adopting arbitrary pre-existing group 7 at CombatMove start. |
| EV-195 | The pinned SDK and tested Game binary expose a persistent outer ScriptFunction execution layer above CombatMove. `gCScriptRoutine_PS::GetSPU()` reaches the actor `gCScriptProcessingUnit`, whose `m_StateStack` stores `gScriptRunTimeSingleState` entries including script name, State-vs-Function flag, break block, `m_pArguments` and local callback. In tested `gCScriptAdmin::RunScriptFunction` at `Game +0x1604E0`, the registered ScriptFunction is invoked at `+0x1605E9`; a true result takes the top-frame removal path via the helper at `+0x1627B0`, whereas a false result returns without removing the frame. The runner receives the exact SPU explicitly, and the SPU exposes `GetSelfEntity()` for its actor. `gCScriptAdmin` does not expose a documented public accessor that can safely substitute for that explicit per-dispatch SPU. | CONFIRMED FOR PINNED SDK STRUCTURE + TESTED GAME BINARY | pinned `Georgeto/gothic3sdk@90bfd344de4510dda7ac9da7461cc7f1eac911f7`, especially `ge_scriptprocessingunit.h` / `ge_scriptadmin.h`; tested Game disassembly around `+0x1604E0–+0x160605`; static lifecycle finding commit `27d7768cfa7a8d0d06cc178abe08505e9a15ee8e`, 2026-08-29 | The live outer ScriptFunction frame is a plausible native lifetime boundary that can predate CombatMove. `RunScriptFunction` remains generic infrastructure and must not become combat ownership or unconditional cleanup by itself. |
| EV-196 | C1-O1 runtime confirmed the useful outer ScriptFunction lifetime and also disproved treating any raw frame pointer as a permanent execution ID. Representative GetUpAttack retained the same SPU, `_AI_GetUpAttack` ScriptFunction and non-null `m_pArguments` across the pre-CombatMove offensive request, later CombatMove initial invocation and later cleanup while the break block advanced. Repeated GetUp executions used different live argument pointers. Clean Normal/Whirl likewise retained their attack ScriptFunction through CombatMove/offense/cleanup and removed it before the later enclosing state transition. Bad full Whirl still had `_AI_WhirlAttack` at `AI_SET_STATE_BEFORE`, while `AI_SET_STATE_AFTER` no longer contained that frame and no cleanup occurred between those observations. The run also showed an `m_pArguments` address reused by a later different ScriptFunction after the earlier frame had retired; stack-entry addresses are likewise diagnostic-only. Null argument records were observed for ScriptStates, while the tested attack ScriptFunction snapshots used non-null arguments. | CONFIRMED FOR TESTED PLAYER C1-O1 MATRIX; RAW POINTER UNIQUENESS REJECTED | C1-O1 implementation `54a64552514f4d6795b4a51cbad7848b5df3795b`; raw `research/raw/2026-08-29_c1o1_outer_scriptfunction_identity_probe.log`, commit `adf6a6102007c56f7502b01072acbe00bcbc35a4`; derived `research/archive/2026-08-29_c1o1_outer_scriptfunction_identity_probe_connector_extract.txt`, commit `5da094a0c2d6fc58fe260933799a76e43ef7d1d3`, source SHA256 `568775FE823071BBCF0593B0D6370CB7C4B32865EAE7996E3E2A242CC6438DFA`; interpreted current-state commit `9a5cacce659768162687300298c6bfe1fb7ea887`, 2026-08-29 | Use the native frame only as a lifetime-bound correlator: exact SPU + live ScriptFunction context + non-null arguments + same ScriptFunction name, with explicit retirement before pointer reuse. Keep C1's monotonic generation as the durable plugin-side identity. If a future relevant ScriptFunction has null arguments, fail diagnostically rather than guessing another identity rule. |

## Current Research Consequence

B8/B9 established that the known stale class generalizes across tested held-Use2 attacks and that destructive AISetState occurs after the native cleanup opportunity has been lost on the bad path, while clean/reaction paths fulfill cleanup before that point. C1 then validated the consequence-based per-source obligation model in shadow form, including inherited `7 -> 7`, but exposed that CombatMove begins too late to acquire every legitimate execution.

C1-O1 closes the outer-lifetime identity gate strongly enough to proceed without another broad identity probe:

```text
outer attack ScriptFunction already live
→ legitimate equipped-weapon offense may occur before CombatMove
→ same live frame can persist into later CombatMove and cleanup
→ destructive AISetState can remove that frame without cleanup
→ raw arguments/frame addresses may be reused only after retirement
```

The current technical gate is **C1-O2 shadow outer ScriptFunction binding integration**. The frozen direction is to preserve C1's monotonic generation as durable identity, use a minimal/nesting-safe `RunScriptFunction` dispatch context only to bind a live native frame and acquire legitimate pre-CombatMove weapon offense, reuse the same generation when later CombatMove belongs to that still-live frame, and retire the native binding on true ScriptFunction return before pointer reuse. Cleanup remains source/consequence based and existing AISetState `WOULD_REPAIR` shadow finalization remains unchanged.

Production physical repair remains disabled. `RunScriptFunction` return, FullStop and AISetState are not unconditional cleanup authorities. No family/action/input classifier, arbitrary group-7 adoption, polling/timers/scans, null-arguments fallback, or marker-core simplification is authorized by this evidence.
