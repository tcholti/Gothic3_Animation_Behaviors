# Session Entry Point

**Purpose:** Minimal durable front door for current Gothic 3 Animation Behaviors work. Read this first; retrieve deeper authorities only when the active responsibility requires them.

**Active development branch:** `docs/collision-source-evidence`  
**Stable branch:** `main`  
**Updated:** 2026-09-15

> **WORK BUILD RULE:** Unless a frozen task explicitly authorizes Work to build, Work must not invoke or probe build tooling. Source/static audit -> publish -> STOP. Local build belongs to User + Normal Chat after independent source review.

> **MANDATORY FEATURE-DEVELOPMENT RULE:** Unknown behavior is researched in an isolated probe, not accumulated in `EngineBridge` or another stable module. Proven behavior is promoted into the permanent module that owns the responsibility. Hooks remain transport-only, temporary probe scaffolding is removed, and broad/final validation waits until known scope-changing design decisions are resolved. Full authority: `docs/FEATURE_DEVELOPMENT_METHOD.md`.

> **RELEASE-PURITY RULE:** `Script_FrameCollisionTest` is diagnostic research only. `Script_FrameCollisionBehaviorTest` is the diagnostics-free behavior twin. The eventual shipped plugin remains `Script_G3AnimationBehaviors`; proven collision responsibilities must be deliberately promoted into permanent release-owned modules while `PhysicalFistProbe` and diagnostic scaffolding are excluded/removed.

> **MAX-CONTEXT / FAILED-CHAT RULE:** If a Normal Chat ends before evidence closure, use `docs/PROJECT_OPERATING_PROCEDURES.md` §13 POP-11 and enter Recovery Lock before new implementation/runtime/Work. Oversized runtime logs use §9 POP-07.

Immediate handoff: `docs/BETWEEN_CHATS.md`  
Current frozen probe authority: **none — Normal Chat causal planning is current**  
Latest canonical evidence: `docs/EVIDENCE_LEDGER_283_ONWARD.md` through corrected **EV-285**  
Authoring semantics: `docs/ANIMATION_RULES.md`  
Evidence routing: `docs/EVIDENCE_INDEX.md`  
Local paths: `docs/LOCAL_WORKSTATION_PATHS.md`

---

## Current Checkpoint

```text
raw55 Quick       CLOSED/PASS through EV-273
raw55 true Power  first-contact CLOSED/PASS through EV-276
raw55 Normal      first-contact CLOSED/PASS through EV-279
raw55 Sprint      first-contact CLOSED/PASS through EV-282
raw55 two-FIST cross-family runtime CLOSED as evidence — EV-283

OPEN BEFORE PERMANENT RAW55 DESIGN:
    Normal pre-StatePosition FIST ownership
    non-Quick repeated-FIST contact/rearm semantics
    Sprint-origin same-C1 Action9 -> Action2 repeated-marker continuity

New Balance 0.7:
    source-level preflight PASS — EV-284
    User confirms current downloaded distribution includes Script_AttackCollision
    combined coexistence already proven for marked Whirl and Pierce fixtures — EV-035/EV-150/EV-242
    corrected compatibility interpretation — EV-285
    broad current-bundle regression is DEFERRED until permanent collision structure exists

Permanent collision sequence:
    causal closure
    -> permanent architecture synthesis / implementation
    -> standalone regression
    -> New Balance 0.7 bundle regression with AttackCollision included
    -> collision module complete
    -> Raise / speed-control work
```

Raw8 FIST remains a separate proven mechanism and must not be altered by raw55 research.

No permanent raw55 implementation is authorized yet.  
No Work/source task is currently frozen.  
Recovery Lock remains **CLOSED**.

---

## Current Normal-Chat Responsibility

The next unresolved causal boundary should be **Normal pre-StatePosition FIST**.

EV-283 repeatedly shows legitimate Normal FIST delivery while:

```text
StatePosition=0
RIGHT PhysicalFist/raw55 group5
no Normal early-suppression proof yet
```

The current temporary Normal activation probe only acts after its later suppression proof exists and while `StatePosition == 1`, so the first authored occurrence is ignored.

Relevant precedent:

```text
EV-268  Quick exposed the same pre-proof/pre-state ordering gap
EV-269  Quick proved an authored SP0 FIST can own activation/rearm while later native callback progression survives
EV-279  Normal first contact at SP1 already works with ClearTriggeredList=0
```

Therefore the next causal question is narrow:

> Can a legitimate Normal FIST at StatePosition 0 directly open the exact current RIGHT PhysicalFist/raw55 source while leaving the untouched native Normal callback free to perform its later StatePosition progression and native cleanup?

Do not automatically add `ClearTriggeredList()`. Unlike Quick, Normal has already produced native first-contact damage after authored opening with `ClearTriggeredList=0`; the first probe should change only the ordering/ownership boundary necessary to let the SP0 marker act.

After that boundary is closed, test non-Quick repeated FIST separately. Sprint Action9 -> Action2 continuity remains an execution-identity constraint on that later repeated-marker design, not a separate reason to create family-local ownership.

---

## Deferred Final New Balance Gate

The current New Balance 0.7 source review was useful only to detect whether 0.7 introduced a new collision mechanism that would force redesign. None was identified in New Balance itself; the relevant overlap remains the bundled AttackCollision surface already known from earlier testing.

Do **not** interrupt raw55 research for another compatibility run now.

When the permanent collision subsystem exists, validate the product in this order:

```text
1. clean standalone regression of permanent collision module
2. install New Balance 0.7 exactly as distributed
3. keep bundled Script_AttackCollision active
4. run broad representative marker regression
5. verify no premature timer opening / wrong source / extra rearm / cleanup divergence
6. close collision module
7. begin Raise / speed-control work
```

Torch+1H exact-slot semantics remain intentional: LEFT may legitimately be the attacking torch source when the authored animation strikes with it.

---

## Still Paused

```text
NO permanent raw55 architecture freeze yet
NO production PhysicalFistCollision
NO copying PhysicalFistProbe scaffolding
NO assumption about non-Quick repeated ClearTriggeredList
NO ignoring Normal SP0 marker delivery
NO New Balance final bundle regression before permanent collision structure
NO Raise / speed-control implementation before collision module final gate
NO broad native-creature certification continuation
NO Axe/Rapier compatibility sequence
NO AttackContinuationProtection work
```
