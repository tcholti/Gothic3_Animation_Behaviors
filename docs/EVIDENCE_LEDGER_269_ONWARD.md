# Gothic 3 Animation Behaviors — Evidence Ledger: EV-269 Onward

**Status:** Canonical continuation after `EVIDENCE_LEDGER_265_ONWARD.md` EV-268  
**Date:** 2026-09-13

**ID range:** EV-269 onward

## Purpose

Continue the single global monotonic evidence namespace after EV-268. This split is storage/retrieval only; it does not create a new evidence namespace.

> **Date note:** several September 13 runtime artifacts were accidentally named with `2026.09.12` by the User. The filenames are retained for provenance; where relevant below, their actual runtime date is September 13, 2026.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-269 | The raw55 Quick pre-StatePosition FIST probe closes the frame-1 ordering gap identified by EV-268. With `G3AB_COL_FIST` authored on frame 1, marked Troll Quick reaches the marker while `StatePosition=0`; the diagnostic path requests exact RIGHT `TrollFist` / PhysicalFist/raw55 `5 -> 7`, verifies group7, and calls `TouchDamage.ClearTriggeredList()` once. The later untouched native Quick callback then naturally performs a `7 -> 7` request while advancing `StatePosition 0 -> 1`. Actual `OnDamage` against `PC_Hero` follows when contact occurs, and native cleanup returns the same source `7 -> 5`; C1 cleanup is fulfilled with no terminal repair or lifecycle issue. The User observed frame-1 marked Quick damage at both very close and ordinary range. | CONFIRMED RAW55 QUICK FRAME-1 PRE-STATE AUTHORED ACTIVATION / REARM / DAMAGE / CLEANUP PASS | Diagnostic implementation `0bfb2ba13ba91488570a41592504ac84428def2a`; `research/raw/2026.09.12_troll_quick_fist_activation_frame_1_test2.log`, upload commit `23063ded46487ba7df9a903363a26739575d0f05`; corroborating repeated-marker run `research/raw/2026.09.12_troll_quick_fist_activation_frame_1_frame15.log`, upload commit `cc2573e05c4953680f0813be8df2f30dd6be949d`. Both `2026.09.12` filenames were actually generated September 13. | The raw55 Quick marker mechanism must not require prior native StatePosition progression or prior EV-265 suppression proof. A legitimate early FIST can own the raw55 activation/rearm while native Quick later performs its independent state progression. Preserve the later native `7 -> 7` and native `7 -> 5` cleanup. This remains raw55-specific evidence and does not itself authorize permanent implementation. |
| EV-270 | The frame-1 + later-FIST raw55 Quick control shows that a later FIST frame effect can still reach the diagnostic boundary in the same Quick C1 after the first pre-state FIST, but the current temporary `PhysicalFistProbe` deliberately prevents a second probe-owned intervention once `QuickPreStateFistIntervention::interventionUsed` is set for that actor/source/C1. Therefore the User's observation that the later marker did not produce a second rearm is a temporary probe-policy limitation, not evidence that Gothic cannot deliver a later FIST or that raw55 cannot be rearmed again. | CONFIRMED TEMPORARY RAW55 QUICK ONE-SHOT PROBE LIMITATION; REPEATED AUTHORED FIST SEMANTICS STILL OPEN | `research/raw/2026.09.12_troll_quick_fist_activation_frame_1_frame15.log`, upload commit `cc2573e05c4953680f0813be8df2f30dd6be949d`; source `prototypes/Script_FrameCollisionTest/PhysicalFistProbe.cpp` at implementation `0bfb2ba13ba91488570a41592504ac84428def2a`. Filename date is accidental; actual runtime date September 13. | Do not carry the diagnostic one-shot rule into final raw55 authoring semantics. If raw55 becomes production-supported, each legitimate authored FIST occurrence must be evaluated as its own rearm opportunity, consistent with the project's marker vocabulary. Prove repeated raw55 Quick rearm before permanent promotion. |
| EV-271 | Early-frame authoring controls prove that frame 0 and frame 1 are both real, usable marker frames for the two established production mechanisms tested. Equipped 2H Normal with `G3AB_COL_RIGHT` at frame 0 and at frame 1 both show marked ownership, native timing suppression, accepted RIGHT, exact Flamberge `5 -> 7`, one triggered-list clear, positive `OnDamage` against Golem, native `7 -> 5` cleanup, and clean C1 finalization with no marker rejection or lifecycle issue. Raw8 Sabretooth Quick with `G3AB_COL_FIST` at frame 0 and frame 1 likewise shows initial native-opportunity close, accepted FIST, latch `1 -> 0`, matched early timing permission, permission consumption, and positive native `Game.dll+0x16E348` damage, including transformed-player Quick attacks damaging the spawned Sabretooth. No `REJECTED` marker records were found in either Sabretooth run. The User clarified that the extra frame-0 misses were observed only while the spawned NPC Sabretooth attacked `PC_Hero`; after transforming, the User's own frame-0 Quick attacks hit the spawned Sabretooth every time. The logs show that the frame-0 raw8 marker itself is accepted and its timing permission is armed/consumed even on NPC executions that do not subsequently damage, so the extra misses are not marker-transport failure and are consistent with attack/contact timing rather than a frame-0 authoring defect. | CONFIRMED FRAME 0 + FRAME 1 MARKER TRANSPORT/ACCEPTANCE FOR TESTED EQUIPPED AND RAW8 MECHANISMS; RAW8 NPC FRAME-0 MISS VARIATION IS CONTACT/TIMING, NOT MARKER FAILURE | Runtime upload commit `bdb9522f10ea4327512f0dbd7d5a0f51588e50d0`; `research/raw/2026.09.12_2h_normal_activation_frame_0.log`, `research/raw/2026.09.12_2h_normal_activation_frame_1.log`, `research/raw/2026.09.12_sabertooth_quick_fist_activation_frame_0.log`, `research/raw/2026.09.12_sabertooth_quick_fist_activation_frame_1.log`. All four `2026.09.12` filenames were actually generated September 13; User clarification 2026-09-13 that transformed-player frame-0 Quick hit consistently. | Do not impose a global "frame 1 minimum" rule. Marker frame indices are literal and frame 0 is supported where the factual attack/C1 context already exists. Authoring guidance must remain mechanism-specific: equipped source markers open a physical source window and may legitimately begin at frame 0; raw8 FIST rearms one native opportunity, so placement should be chosen for the intended contact frame. The NPC frame-0 miss variation does not justify treating frame 0 as unreliable. Raw55 remains research-only and must earn equivalent repeated-marker/family semantics separately. |
| EV-272 | The revised raw55 Quick repeated-FIST implementation does not yet test the repeated rearm primitive because its later-FIST intervention never executes. In the two-FIST frame-1/frame-15 Troll Quick runtime, the first FIST still follows EV-269 (`5 -> 7` plus one `ClearTriggeredList()`), the untouched native Quick later performs `7 -> 7` and advances `StatePosition 0 -> 1`, and the later FIST is visibly delivered in the same C1 at `StatePosition=1` while the factual RIGHT `TrollFist` / PhysicalFist/raw55 source is still group7. However there is no `CORE RAW55_QUICK_REPEAT_FIST_REARM_PROBE` record anywhere in the run, so the probe's second `ClearTriggeredList()` never executes. The User observes exactly one damage event per Quick attack and no apparent second rearm. This cannot be interpreted as failure of `ClearTriggeredList()` to rearm raw55, because the causal intervention was never reached. Separately, the non-damaging attacks using the motion named `...PowerAttack...` in this run are `Action=9 / Family=SPRINT`, where the old raw55 whole-callback suppression remains active; this does not contradict the earlier damaging true `Action=2 / Family=POWER` observation. | CONFIRMED LATER-FIST DELIVERY + GROUP7 / REPEAT-INTERVENTION ELIGIBILITY REJECTION; REPEATED REARM PRIMITIVE STILL UNTESTED | Implementation `a68361a48662f4236e2d742057aa64bde2d03346`; runtime `research/raw/2026.09.13_troll_quick_fist_activation_frame_1_frame_15_test_2.log`, upload commit `1d9b11a375bbc760dfd9b70ed977af19765228e5`; comparison to earlier `research/raw/2026.09.12_troll_quick_fist_activation_frame_1_test2.log` (actual runtime date September 13) for `Action=2 / Family=POWER`. | Do not alter the rearm primitive yet. First observe which revised eligibility predicate rejects the later raw55 FIST. The next diagnostic should be observability-only inside `PhysicalFistProbe`: expose the stored first-FIST proof/C1/source identity, current C1/source identity, scanned FIST count, current StatePosition/group/use type, and one concise eligibility classification at the later FIST. No stable marker-engine change and no additional collision/damage intervention are justified by EV-272. |
| EV-273 | The observability-only eligibility run does not reproduce EV-272's repeated-FIST rejection and positively proves the raw55 Quick repeated-FIST rearm primitive. For repeated Troll Quick executions with authored FIST at frames 1 and 15, the later frame-15 dispatch reports `CurrentC1Valid=1`, matching current/stored C1 and RIGHT source, PhysicalFist/raw55 group7, first intervention/proof present, `DecisionFistCount=2`, no mixed source markers, `RequiredSourceMask=0`, raw8 resolver absent, `QuickHit=1`, `Eligible=1`, and `FirstFailedGate=NONE`. The unchanged repeated-FIST helper then immediately logs `ClearTriggeredList=1 / REPEAT_FIST_REARM=1`. Multiple Quick executions show an `OnDamage` before the later marker and a second `OnDamage` after that rearm while the same Troll Quick C1 remains active, matching the User's observation that Quick can damage once per authored FIST marker. The same run preserves the temporary family controls: true `Action=2 / Family=POWER` remains `SuppressNative=0` and can damage, while Normal remains whole-callback suppressed. Because the only source change since EV-272 was diagnostics-only logging, the exact EV-272 eligibility rejection is not identified by this run; it is a non-reproduced prior runtime condition rather than evidence against the rearm primitive. | CONFIRMED RAW55 QUICK REPEATED-FIST ELIGIBILITY + SECOND TRIGGERED-LIST REARM + SECOND DAMAGE PASS; EV-272 REJECTION NOT REPRODUCED | Observability implementation `3d9a659e6ac66b13d287ec91c80a04278a65cb17`; startup/load artifact `research/raw/2026.09.13_deployment_test.log`; runtime `research/raw/2026.09.13_troll_quick_fist_frame_1_frame_15_repeat_fist_eligibility.log`; upload commit `6cc1159c8f302ff600b96dc4eabc8f76167bc058`; User runtime observation 2026-09-13. | Repeated raw55 Quick authored-FIST rearm is no longer an open primitive question. Do not relax or redesign the proven eligibility/rearm path based on EV-272. Preserve the factual two-occurrence behavior and native Quick progression/cleanup. Raw55 remains research-only: the next responsibility should be chosen from the still-open family/ownership questions before permanent generic occurrence integration or `PhysicalFistCollision` promotion. |
