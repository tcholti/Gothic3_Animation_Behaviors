# Gothic 3 Animation Behaviors — Evidence Ledger: C1 Hook-Transport Continuation

**Status:** Canonical continuation after `EVIDENCE_LEDGER_STEP_B.md` EV-198  
**Date:** 2026-08-30  
**ID range:** EV-199 onward

## Purpose

This file continues the collision-lifecycle evidence after EV-198 while the C1-O2 outer-frame binding gate is developed through directly relevant hook-transport hardening, lightweight dispatch correlation and bounded ownership integration. The same promotion rule applies: source facts, runtime facts, user observations and design consequences remain separately qualified.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-199 | C1-O2 direct `RunScriptFunction` dispatch capture and its first recursion-safe `.ThisCall()` correction crashed in equivalent registered-ScriptFunction paths ending around the tested `Game +0x1605EB` return point. Reducing `RunScriptFunction` to explicit-this recursion-safe pure pass-through removed all C1-O2 dispatch/lifecycle work and passed bounded load/idle isolation, but extended gameplay later produced a **different** crash through the pre-existing AISetState hook path (`Script.dll +0x12F61`, `Game +0x1604D3`). Source audit found AISetState still used the SDK hook helper's shared legacy `GetSelf` ThisCall transport. Converting AISetState alone to explicit per-invocation `gCScriptRoutine_PS *this` plus the recursion-safe `.ThisCall()` builder preserved ordering/C1 semantics and then passed about 329.9 seconds of varied gameplay with normal unload: 172 AISetState records, 43 AIFullStop records and 62 C1 finalizations, zero tracked outer-binding/generation/null-argument failure signals, and one confirmed expected log-only bad-Whirl `WOULD_REPAIR`. | CONFIRMED SOURCE/HOOK-ISOLATION SEQUENCE AND TESTED AISETSTATE TRANSPORT STABILITY; ORIGINAL C1-O2 DISPATCH-CAPTURE CRASH REMAINS UNRESOLVED | C1-O2 crash evidence retained under active `research/raw/2026-08-29_c1o2_*`; AISetState implementation `8024d846eed626725ca46fa744a79af44fcb2815`; archived stability raw `research/archive/2026-08-29_c1_aisetstate_recursion_safe_extended_gameplay_stability.log`; derived extract `research/archive/2026-08-29_c1_aisetstate_recursion_safe_extended_gameplay_stability_extract.txt`; raw evidence commit `6b4cda21466ccca6c42a9c51b98fbbfe6da48ed3`; extract commit `86308b1c91176501c294ee50af99ce9bb418900d`, 2026-08-29 | Treat legacy shared-`GetSelf` member-hook transport on directly relevant paths as a real substrate risk. Do **not** infer that making AISetState stable solved the earlier `RunScriptFunction` dispatch-capture crash. Keep `RunScriptFunction` pure pass-through until the prerequisite hook substrate is deliberately reassessed. |
| EV-200 | AIFullStop was converted in isolation from legacy shared `GetSelf` ThisCall transport to explicit per-invocation `gCScriptRoutine_PS *this`, forwarding that exact pointer to the original exactly once through `.ThisCall()` while preserving `_ReturnAddress`, caller/context stack capture, primary-motion snapshot, current-state/input/duration diagnostics and their pre-original ordering. Independent Normal Chat source review passed. The built/deployed DLL SHA256 was `89329667BF83479E419C2775965E0CC41769F2BFD247F84C95939B673292B13B`; isolated startup/unload passed. Extended gameplay then remained stable through broad combat/traversal and repeated user-confirmed bad-skip reproduction across 1H, Shield+1H, Dual 1H, Staff and 2H. The canonical run recorded 127 AIFullStop calls, 71 CombatMove FullStops, 513 AISetState calls, 179 C1 finalizations and 16 `WOULD_REPAIR` source outcomes, then unloaded normally. All 11 generic `C1 INVARIANT WARNING` records were the already-known `UNOWNED_PLAYER_OFFENSE_REQUEST` acquisition gap from EV-194. Extracted counts were zero for `C1-O2 BINDING INVARIANT`, `OUTER_RETURN_OUTSTANDING`, `LIVE_FRAME_MISMATCH`, `PRECOMBAT_GENERATION_FRAME_OVERLAP`, `OVERLAP_OUTSTANDING`, `CANDIDATE_GENERATION_CHANGED`, `FINALIZATION_GENERATION_CHANGED`, `NULL_ARGUMENTS` and `UNRESOLVED_NOT_EQUIPPED`. Physical repair remained disabled. | CONFIRMED FOR TESTED AIFULLSTOP TRANSPORT SOURCE CONTRACT + EXTENDED GAMEPLAY STABILITY | implementation `18ff183e05b599de9e035722b15e3bfb6cbbc034`; independent Normal Chat source review PASS; archived raw `research/archive/2026-08-29_c1_aifullstop_recursion_safe_extended_gameplay_stability.log`; derived `research/archive/2026-08-29_c1_aifullstop_recursion_safe_extended_gameplay_stability_connector_extract.txt`; evidence commit `72273ead0ae7821a0d70b2e3b168ad00a1271f1a`; local pre-Git-normalization raw SHA256 `37C55A44574F75E4323AD7D316898C4CBDF126929F1141BA2D6E2EB8D79BD0FE`, 2026-08-29 | AIFullStop is stable enough for the tested hook-transport prerequisite and still preserves the known destructive stress case as shadow evidence. This does **not** authorize unconditional FullStop cleanup, held-Use2/2500-ms classification, physical repair, or automatic C1-O2 resumption. Reassess the remaining directly relevant hook substrate before reintroducing dispatch capture. |
| EV-201 | `SetCollisionGroup` was converted in isolation from shared `GetSelf<eCEntity *>()` transport to explicit per-invocation `eCEntity *this` plus `.ThisCall()` at tested `Engine +0x225660`, with the reviewed before/original/after/marker-retirement/C1-observation/diagnostic ordering preserved. Build, exact DLL deployment/hash identity and isolated load/unload all passed; validated DLL SHA256 was `C5D85C44A751C68BB956C2F1D6C9ABE6E889BDBB3363FDC328158F2933DA0397`. The extended gameplay stability run then **crashed** after extensive mixed combat/interruption coverage. Whole-run indexing of the 5,009,715-byte / 164,125-line canonical raw log found 6,313 diagnostic event blocks, including 307 `ENGINE SetCollisionGroup`, 175 C1 finalizations, 130 C1 offense requests and 107 C1 cleanup fulfillments. There were **23 actual finalization `WOULD_REPAIR` source outcomes**; the earlier raw string count of 24 included the startup banner. All seven C1 invariant warnings were `UNOWNED_PLAYER_OFFENSE_REQUEST`, the already-known pre-CombatMove/GetUpAttack acquisition gap. Clean/native cleanup, pre-activation interruption, armed destructive abandonment, inherited stale `7 -> 7`, and source-specific Dual LEFT/RIGHT controls remained logically consistent with the established C1 model. In the final destructive Staff Whirl, generation 275 recorded `Wrestling Staff_BBM` RIGHT as `WOULD_REPAIR`, `ActualGroup: 7`, `CleanupObserved: 0`, `StillEquipped: 1`, with `PhysicalCollisionChanged: 0`; a following ambient snapshot still saw that Staff at group 7 and no later native cleanup appeared before the runtime log ended roughly seven seconds later. The User independently observed that this stale armed state could damage actors by contact and collide with world objects while merely moving with the weapon held. The crash log recorded `EXCEPTION_ACCESS_VIOLATION` at tested `Engine +0x225650`, with a summarized stack including `Script_FrameCollisionTest +0x10802`, `Script.dll +0x12F61` and `Game +0x1604D3`; `EAX=FFFFFFFF` at the fault. Initial interpretation tentatively localized the crash near the SetCollisionGroup wrapper's own getter observations, but EV-202 supersedes that root-cause interpretation using the exact crashed DLL disassembly. | CONFIRMED EXTENDED RUN CRASH + CONSISTENT C1 SHADOW CLASSIFICATION; ORIGINAL ROOT-CAUSE INTERPRETATION SUPERSEDED BY EV-202 | implementation `fbb300065af2f9119531e06949eac59c2586a519`; crash/raw evidence commit `c4de7f37d1b8f0844be05afc7fb2a42f09b29ce3`; canonical raw `research/archive/2026-08-29_c1_setcollisiongroup_recursion_safe_extended_gameplay_stability.log`, SHA256 `91D15FD54A51B0DC4AC4067E776757F813B934FD236C295D0CD1F793E540DAD5`; crash logs archived under `research/archive/ge3log9.log` and `research/archive/Lastlog_GE3.log`; first compact extract commit `4818e24fc2b79024fd247975c06ee266b522fa07`; whole-run derived package commit `9dae0a03426101f572357f74700ed441a75b2fbb`; tested binary reference `builds/current_tested/modules/Engine/disassembly/part_0143.txt`, 2026-08-29 | Do not use EV-201 alone to roll back SetCollisionGroup or to diagnose a transport failure. EV-202 provides the later exact DLL-level localization. C1-O2 remains suspended and physical repair remains disabled. |
| EV-202 | Reassessment of the EV-201 crash identified the direct failing call path. The SDK hook helper's `.ThisCall()` builder is explicitly recursion-safe: it pushes incoming `ECX` as the hook's explicit this argument and restores the same value to `ECX` before the relocated original. Tested Engine disassembly independently confirms `Engine +0x225660` is a normal one-stack-argument thiscall-style `SetCollisionGroup` entry (`ECX` saved as the entity, stack argument used as the requested group, `ret 4`). The crash dump's machine stack contained immediate return address `0x50DAD839` (`Script_FrameCollisionTest` RVA `0xD839`), which the summarized crash stack had skipped. The User ran `dumpbin` against the **exact crashed DLL** after verifying SHA256 `C5D85C44A751C68BB956C2F1D6C9ABE6E889BDBB3363FDC328158F2933DA0397`. DLL disassembly shows RVA `0xD833` calls imported `eCEntity::GetCollisionGroup()` on the remembered C1 source inside `CollisionLifecycleGuard::FinalizeAfterAISetState()`, and RVA `0xD839` is the instruction immediately after that call. The higher reported DLL RVA `0x10802` is likewise the instruction immediately after the AISetState wrapper calls `FinalizeAfterAISetState()`. Source confirms that finalization currently executes native AISetState first, then iterates remembered raw `sourceInstance` pointers and calls `source.sourceInstance->GetCollisionGroup()` before classifying `NO_OP` / `WOULD_REPAIR` / `UNRESOLVED_NOT_EQUIPPED`. Therefore the observed crash is directly localized to **post-AISetState C1 finalization dereferencing a remembered source whose liveness was not established**, not to the SetCollisionGroup wrapper's pre/post getter observations. Separately, the User performed a bounded control run with the research DLL removed: deliberately produced stale offensive collision remained active through ordinary running and repeated contact with many NPCs, damaged them and provoked attacks, but did not crash during roughly five minutes or more of testing. Ordinary running did not clean the stale collision, supporting the absence of a simple movement-end or short timeout cleanup in that observation window. This control does not prove stale collision can never contribute to a crash, but it weakens the claim that stale group 7 alone explains EV-201. | CONFIRMED EXACT DLL CRASH-CALL LOCALIZATION; SETCOLLISIONGROUP TRANSPORT NOT SHOWN TO HAVE FAILED; BOUNDED NO-DLL STALE-COLLISION CONTROL OBSERVATION | exact crashed DLL SHA256 above; crash stack/raw artifacts from EV-201; tested SDK `util/RtPatch/include/g3sdk/util/Hook.h` and `util/RtPatch/src/Hook.cpp`; tested Engine disassembly `builds/current_tested/modules/Engine/disassembly/part_0143.txt`; User-provided `dumpbin /disasm:nobytes` windows around exact DLL RVAs `0xD839` and `0x10802`; source `prototypes/Script_FrameCollisionTest/CollisionLifecycleGuard.cpp`, 2026-08-30 | Reclassify the SetCollisionGroup extended gate as **interrupted by an independently identified C1 finalization source-liveness crash**, not FAIL/CRASH of SetCollisionGroup transport. Do not roll back SetCollisionGroup on this evidence. Next implementation must make `FinalizeAfterAISetState()` source-liveness-safe before dereferencing remembered sources, preserving the shadow classification model and keeping physical repair disabled. |
| EV-203 | The bounded `FinalizeAfterAISetState()` source-liveness correction was implemented at `e7d81f7b3cd08199e10ad98a5270e0cc8167c748`: current equipped identity is established by pointer comparison before remembered-source observation, non-current remembered sources are not dereferenced for name/group diagnostics, and physical repair remains disabled. Independent source review passed. The validated DLL SHA256 was `E18C35894B5C813C80F30D51DF76972BF1B10011B7A102CE29551210E54303B5`; isolated load/unload passed. The extended mixed-gameplay run then completed normally for about 852.7 seconds with 356,139 source lines and 13,723 indexed event blocks, including 992 `ENGINE SetCollisionGroup`, 333 C1 finalizations, 235 AIFullStop calls, 147 CombatMove FullStops and 195 native cleanup call sites. All nine C1 invariant warnings were the already-known `UNOWNED_PLAYER_OFFENSE_REQUEST` acquisition-gap family. The final deliberate stale-state stress case reached finalization with `WOULD_REPAIR`, `LivenessEstablished: 1`, `OutstandingBeforeFinalization: 1`, `ActualGroup: 7`, `CleanupObserved: 0`, `StillEquipped: 1`, and `PhysicalCollisionChanged: 0`; the indexed tail continued afterward without a later cleanup for that final source. No crash reproduced. The run did not produce an `UNRESOLVED_NOT_EQUIPPED` signal, so runtime exercise of the exact `LivenessEstablished=0` outstanding branch is not claimed. A reaction/knockdown sequence also showed a previously stale source back at equipped group 5 before a later GetUpAttack reactivated it, supporting reaction-side cleanup without isolating its exact owner. User-observed holster/draw cleanup remains plausible but causally unproven. | CONFIRMED TESTED C1 FINALIZER STABILITY + SETCOLLISIONGROUP EXTENDED TRANSPORT STABILITY; LIVENESS-NEGATIVE OUTSTANDING BRANCH NOT POSITIVELY EXERCISED | implementation `e7d81f7b3cd08199e10ad98a5270e0cc8167c748`; isolated evidence commit `0e31c9b12a6df3b05d85364de0688496e826d38c`; archived extended raw `research/archive/2026-08-30_c1_finalization_source_liveness_safe_setcollisiongroup_extended_gameplay_stability.log`, source SHA256 `D6298DA4C7C490FC84BC2A4586CC92A2F8D1A5003B54CC734EC584FECF5CE72B`; derived package `research/derived/2026-08-30_c1_finalization_source_liveness_safe_setcollisiongroup_extended_gameplay_stability_large_log/`; evidence commit `0f663562b15c2dfc3288b4856474912ce2820e26`, 2026-08-30 | Promote SetCollisionGroup explicit-this transport to PASS for tested extended stability and the finalizer correction to PASS for tested stability. Do not infer that the original C1-O2 direct `RunScriptFunction` dispatch-capture crash is solved. Next step is a Normal Chat C1-O2 reassessment from this stabilized substrate, with physical repair still disabled. |
| EV-204 | C1-O2-P1 replaced the failed eager per-dispatch capture pattern with a tiny stack-local `RunScriptFunctionScope` plus one TLS current-scope pointer around the already-recursion-safe explicit-this `RunScriptFunction` hook. Independent source review confirmed the generic path does no engine/API/state-stack/actor/C1/string/container/allocation/logging/collision work before native execution, calls native exactly once with unchanged arguments, restores TLS before return diagnostics, and does not persist the scope/name reference. The corrected P1 startup banner was committed at `2509f3759644a39f180138971791cd741245bf00`; validated built/live DLL SHA256 was `44C15C068D34F82BDCB03EEBEC76C3C0A93094D64662B032954BFF637D51E05D`. P1-B isolated main-menu load/unload completed normally with the exact banner and normal unload. P1-C targeted gameplay completed without crash and produced a 1,056,558-byte / 34,718-line raw log with 1,360 indexed event blocks, 27 `C1-O2-P1 OFFENSE SCOPE` events and four `C1-O2-P1 DISPATCH RETURN` events. Four deliberate GetUpAttack pre-Combat acquisition gaps each showed the initial exact equipped-source `5 -> 7` offense while a live P1 scope existed; scope SPU matched player SPU, runtime stack matched the player SPU state stack, ScriptFunction name was `_AI_GetUpAttack`, parent scope was absent in the tested cases, and `OffenseObservedSet=1`. Independent `OUTER_FRAME OFFENSE` at the same events agreed on `_AI_GetUpAttack` / `OnAI_GetUpAttack` with the same SPU and non-null arguments. In all four cases matching player CombatMove began in that same live ScriptFunction invocation before the wrapper later returned `GEFalse` / suspended; TLS was restored and no scope persisted. The later GetUp timer `7 -> 7` offense occurred after the wrapper scope disappeared while `_AI_GetUpAttack` remained suspended on the SPU. Representative ordinary Normal/Quick timer offense likewise commonly had `CurrentScopeExists=0`, showing that the live wrapper is not a universal offense-owner requirement. The four C1 invariant warnings were exactly the already-known `UNOWNED_PLAYER_OFFENSE_REQUEST` events corresponding to the deliberate GetUp pre-Combat gaps; P1 intentionally did not acquire ownership. A separate P1-D long-duration stability run was not performed, so no independent extended-stability claim is made. | CONFIRMED P1 SOURCE CONTRACT + ISOLATED STABILITY + TARGETED GETUP DISPATCH/OFFENSE/COMBATMOVE ORDERING; EXTENDED P1 STABILITY NOT SEPARATELY CLAIMED | P1 implementation `6e72e4d73cac60d053984bc66db006e87d6151f3`; banner correction `2509f3759644a39f180138971791cd741245bf00`; isolated raw `research/archive/2026-08-30_c1o2p1_lightweight_dispatch_bridge_isolated_load.log`, evidence commit `3b0b498bce4e2d7cd16e8fb56a2764c950663e36`; targeted raw `research/archive/2026-08-30_c1o2p1_player_2h_normal_getup_offense_correlation.log`, raw commit `ef9d2b7378885cdddfc2f95061ab6f1546000145`; source SHA256 `025013C3AE3C612D8311A58E2E4CE35F8F76D020BB4CBEA7F3CB4F8BBCBAA7AD`; derived package `research/derived/2026-08-30_c1o2p1_player_2h_normal_getup_offense_correlation_large_log/`, package commit `85e84afa1a40e7f35c82a9fc4e0000bd532f1ca2`, 2026-08-30 | P1 establishes the lightweight synchronous bridge needed for the missing pre-Combat acquisition point without restoring the old eager capture. The four positive GetUp cases support a smaller next hypothesis: use native ScriptFunction identity only as a temporary bridge from real early offense to matching CombatMove, then consume that binding before `RunScriptFunction` returns/suspends. C1's monotonic generation/source ownership remains durable afterward. Do not assume cross-suspension native binding is required; do not claim it universally unnecessary until P2/later evidence passes. Physical repair remains disabled. |
| EV-205 | C1-O2-P2 implemented the smaller bridge model at `cc51c67c19425be4e4d6a4838803ed3e66b2a071`: only a successful exact equipped-source offense inside the proven P1 live scope may lazily validate the live SPU/runtime-stack/ScriptFunction/non-null-arguments correlator and acquire the existing monotonic C1 generation; matching CombatMove reuses that generation and immediately consumes/retires the temporary native-frame binding, while durable generation/source ownership continues independently. Independent P2-A source audit passed and the corrected startup identity was committed at `8b6a5873b8caa27b469ae831d25bf155a6c7f189`. Validated DLL SHA256 was `1081B287912DB9A368164DDE13542A7EC2D6E5DBB0AA29B04C19BD7932D92C7C`; P2-B isolated main-menu load/unload passed. P2-C targeted 2H testing produced three ordinary Normal controls plus five GetUpAttack executions, generations `20/33/45/54/64`; every GetUp showed `PRECOMBAT_ACQUIRED` → first C1 offense on the same OUTER_BOUND generation → `PRECOMBAT_BRIDGE_CONSUMED` on that same generation before dispatch return → later timer offense on the persisted generation → native cleanup on that same generation/source → clean finalization, with zero unowned or bridge/invariant failures. P2-D then ran broad mixed gameplay for roughly 16 minutes across 1H, Shield+1H, Dual, Staff and 2H with clean attacks, reactions/stumbles/knockdowns, GetUpAttack, GetUpParade, destructive interruptions and state/traversal churn. The canonical 12,123,937-byte / 398,143-line P2-D raw log produced 15,387 indexed event blocks: 306 C1 offense requests, 264 C1 cleanup fulfillments, 436 finalizations, 536 outer-binding events, 157 CombatMove FullStops, 1,112 AISetState calls and 844 SetCollisionGroup events. There were exactly 13 `PRECOMBAT_ACQUIRED`, 13 matching `PRECOMBAT_BRIDGE_CONSUMED`, and 13 dispatch-return events; tested bridge generations were `200/298/314/364/384/473/622/658/660/671/726/857/868`, spanning Staff, Shield+1H and 2H. Every inspected bridge was consumed before wrapper return. The signal index contained no C1 invariant warning, error/exception, failure, mismatch/divergence, stale, unresolved/orphan or unsafe-skip class. It contained 29 actual finalization `WOULD_REPAIR` source outcomes plus the startup banner's literal word. Representative 2H, Staff, 1H/Torch and Dual contexts show the expected model: exact source was genuinely offensive, no native cleanup had occurred, source was still equipped/live at destructive AISetState, shadow classified `WOULD_REPAIR`, and `PhysicalCollisionChanged` remained 0. Dual cases included source-specific RIGHT and LEFT outcomes. As a consistency check, treating each of the 13 GetUp early+timer offense pairs as one source obligation reduces 306 offense-request events to 293 observed offensive obligations; those close exactly as 264 native cleanup fulfillments + 29 outstanding `WOULD_REPAIR` outcomes. Two observed Staff GetUpParade action-31 sequences traversed Hit/Recover without a C1 offense request, supporting consequence-based ownership rather than inference from the filename's `Hit` token. The P2-D file itself ends during continued gameplay/NPC collision activity and does **not** contain the DLL unload line, so P2-D shutdown is not claimed; the User reported the game worked throughout, while P2-B independently proves clean unload for this exact DLL. The User also subjectively observed smoother-feeling runtime in P2-D than several earlier research builds; no controlled performance comparison was performed, so no causal performance improvement is claimed. | CONFIRMED P2 SOURCE CONTRACT + ISOLATED LOAD/UNLOAD + TARGETED GETUP MEANING + BROAD GAMEPLAY OWNERSHIP/STABILITY; P2-D SHUTDOWN NOT CAPTURED | implementation `cc51c67c19425be4e4d6a4838803ed3e66b2a071`; banner `8b6a5873b8caa27b469ae831d25bf155a6c7f189`; P2-B raw commit `b23892ca1aaf54ad3165a76cb05149cf8c73a328`; P2-C raw `research/raw/2026-08-30_c1o2p2_player_2h_normal_getup_bridge_consumption.log`, commit `0697437cf9b3036367e9bdba2ec85576f86ddbe3`; P2-D raw `research/raw/2026-08-30_c1o2p2_broader_combat_interruption_stability.log`, SHA256 `50AE4944C23DB30C78CE0373E241DDAAC32C252C2D8563584871F098C96BB041`, raw commit `f2d54faefbb1e2feccb31ac9abc46a4cf6a8d134`; derived package `research/derived/2026-08-30_c1o2p2_broader_combat_interruption_stability_large_log/`, package commit `680d5068a16034e122aeff2ef5528e9cc16477bf`, 2026-08-30 | Close C1-O2-P2 and the outer acquisition/binding question for the tested model. Live ScriptFunction identity is sufficient as a temporary pre-Combat offense→CombatMove correlator; C1's monotonic generation/source obligation is the durable identity afterward. Do not restore eager dispatch machinery, cross-suspension native binding, family/action/input classifiers or null-argument fallback. Physical repair remains disabled. The next responsibility belongs in Normal Chat: design the smallest controlled native-equivalent repair gate from the now-stable shadow `WOULD_REPAIR` classification, preserving exact-source/liveness/cleanup invariants before any Work implementation is frozen. |