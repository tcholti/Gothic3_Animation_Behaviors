# Gothic 3 Animation Behaviors — Evidence Ledger: EV-274 Onward

**Status:** Canonical continuation after `EVIDENCE_LEDGER_269_ONWARD.md` EV-273  
**Date:** 2026-09-13

**ID range:** EV-274 onward

## Purpose

Continue the single global monotonic evidence namespace after EV-273. This split is storage/retrieval only; it does not create a new evidence namespace.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-274 | The true raw55 Power callback-boundary observation proves compound native ownership inside one original `_AI_PowerAttack` invocation for factual `Action=2 / Family=POWER`. In Troll C1 generation 6, the decisive observed invocation begins with the exact RIGHT `TrollFist` / PhysicalFist raw55 source at `Item_Equipped` group5 and `StatePosition=0`, then the native callback performs the raw55 `5 -> 7` request and returns with `StatePosition=1` and the same source at group7. The boundary record is `SameC1=1`, `SameRight=1`, `Action=2->2`, `Phase=1->1`, `StatePosition=0->1`, `RightGroup=5->7`, `NativeResult=1`. Later invocations remain `StatePosition=1` / group7. The authored FIST is subsequently delivered while the same Power C1 is active; native `OnDamage` against `PC_Hero` follows; native cleanup returns the exact RIGHT source `7 -> 5`; C1 cleanup is fulfilled and finalization is `NO_OP_NO_OUTSTANDING`. The focused local `--clean-first` build recompiled `PhysicalFistProbe.cpp` and linked `Script_FrameCollisionTest.dll`, confirming the observer was present. An earlier same-day runtime artifact without `RAW55_POWER_CALLBACK_BOUNDARY` was generated from stale pre-probe local source and is not evidence about Power eligibility or callback behavior. | CONFIRMED RAW55 TRUE POWER CALLBACK COMPOUND OWNERSHIP: NATIVE STATEPOSITION `0 -> 1` + RIGHT RAW55 `5 -> 7` OCCUR IN THE SAME ORIGINAL CALLBACK INVOCATION | Observation implementation `2a4e4c8bc96823af0f8db8710a8c56e8f890339a`; stale pre-probe artifact `research/raw/2026.09.13_troll_raw55_power_callback_boundary.log` committed before the Work implementation reached the local branch and therefore excluded from the causal result; correct local clean build and runtime committed together as `ab2522ae9d451e8663da95e324dd3621f051fd91`, with build artifact `research/raw/2026.09.13_build log.txt` and runtime `research/raw/2026.09.13_troll_raw55_power_callback_boundary_2.log`; User observation 2026-09-13 that the true-Power-looking attack damaged. | Reject whole `_AI_PowerAttack` suppression as a candidate true-Power production mechanism: it would remove required native StatePosition progression together with the premature raw55 opening. Do not generalize this result to factual Sprint/Action9 merely because Sprint shares `_AI_PowerAttack` transport. The next smallest causal question is selective separability: while the original true-Power callback executes normally, can only its exact scoped RIGHT raw55 `5 -> 7` request be suppressed while native StatePosition still advances `0 -> 1`, with no later raw55 activation or lifecycle divergence? Keep this work diagnostic-only in `PhysicalFistProbe`; `EngineBridge` remains transport-only. |
