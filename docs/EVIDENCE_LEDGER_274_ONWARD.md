# Gothic 3 Animation Behaviors — Evidence Ledger: EV-274 Onward

**Status:** Canonical continuation after `EVIDENCE_LEDGER_269_ONWARD.md` EV-273  
**Date:** 2026-09-13

**ID range:** EV-274 onward

## Purpose

Continue the single global monotonic evidence namespace after EV-273. This split is storage/retrieval only; it does not create a new evidence namespace.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-274 | The true raw55 Power callback-boundary observation proves compound native ownership inside one original `_AI_PowerAttack` invocation for factual `Action=2 / Family=POWER`. In Troll C1 generation 6, the decisive observed invocation begins with the exact RIGHT `TrollFist` / PhysicalFist raw55 source at `Item_Equipped` group5 and `StatePosition=0`, then the native callback performs the raw55 `5 -> 7` request and returns with `StatePosition=1` and the same source at group7. The boundary record is `SameC1=1`, `SameRight=1`, `Action=2->2`, `Phase=1->1`, `StatePosition=0->1`, `RightGroup=5->7`, `NativeResult=1`. Later invocations remain `StatePosition=1` / group7. The authored FIST is subsequently delivered while the same Power C1 is active; native `OnDamage` against `PC_Hero` follows; native cleanup returns the exact RIGHT source `7 -> 5`; C1 cleanup is fulfilled and finalization is `NO_OP_NO_OUTSTANDING`. The focused local `--clean-first` build recompiled `PhysicalFistProbe.cpp` and linked `Script_FrameCollisionTest.dll`, confirming the observer was present. An earlier same-day runtime artifact without `RAW55_POWER_CALLBACK_BOUNDARY` was generated from stale pre-probe local source and is not evidence about Power eligibility or callback behavior. | CONFIRMED RAW55 TRUE POWER CALLBACK COMPOUND OWNERSHIP: NATIVE STATEPOSITION `0 -> 1` + RIGHT RAW55 `5 -> 7` OCCUR IN THE SAME ORIGINAL CALLBACK INVOCATION | Observation implementation `2a4e4c8bc96823af0f8db8710a8c56e8f890339a`; stale pre-probe artifact `research/raw/2026.09.13_troll_raw55_power_callback_boundary.log` committed before the Work implementation reached the local branch and therefore excluded from the causal result; correct local clean build and runtime committed together as `ab2522ae9d451e8663da95e324dd3621f051fd91`, with build artifact `research/raw/2026.09.13_build log.txt` and runtime `research/raw/2026.09.13_troll_raw55_power_callback_boundary_2.log`; User observation 2026-09-13 that the true-Power-looking attack damaged. | Reject whole `_AI_PowerAttack` suppression as a candidate true-Power production mechanism: it would remove required native StatePosition progression together with the premature raw55 opening. Do not generalize this result to factual Sprint/Action9 merely because Sprint shares `_AI_PowerAttack` transport. The next smallest causal question is selective separability: while the original true-Power callback executes normally, can only its exact scoped RIGHT raw55 `5 -> 7` request be suppressed while native StatePosition still advances `0 -> 1`, with no later raw55 activation or lifecycle divergence? Keep this work diagnostic-only in `PhysicalFistProbe`; `EngineBridge` remains transport-only. |
| EV-275 | The true raw55 Power selective-group-suppression probe proves that Power's native StatePosition progression is separable from its premature physical raw55 opening. Two factual Troll executions, C1 generations 6 and 20, each log one exact `CORE RAW55_POWER_GROUP_SUPPRESSION` for `Action=2`, RIGHT `TrollFist` / UseType55, requested group7 from before-group5 at `StatePosition=0`. In the immediately corresponding original `_AI_PowerAttack` callback boundary, `SameC1=1`, `SameRight=1`, `Action=2->2`, `Phase=1->1`, `StatePosition=0->1`, `RightGroup=5->5`, and `NativeResult=1`. Subsequent Power callback observations in each C1 remain `StatePosition=1` and `RightGroup=5->5`; no later raw55 `5 -> 7` appears. The authored `G3AB_COL_FIST` is still delivered at `StatePosition=1` while the exact RIGHT source remains group5, and the stable marker path reports `UNSUPPORTED_MISSING_SOURCE`. No Power `OnDamage` occurs in either execution, matching the User's observation that both Power-animation attacks could not damage. Each Power C1 finalizes with RIGHT `Outstanding=0`, `Cleanup=0`, `Outcome=NO_OP_NO_OUTSTANDING`, proving no fabricated offense obligation, terminal repair, or lifecycle divergence. | CONFIRMED RAW55 TRUE POWER EARLY-GROUP / STATE SEPARABILITY PASS; PREMATURE NATIVE `5 -> 7` CAN BE SUPPRESSED WHILE REQUIRED `STATEPOSITION 0 -> 1` SURVIVES | Selective-suppression implementation `9fdea8a857faf23eefa873b3792bb8da37bf8288`; runtime `research/raw/2026.09.13_troll_raw55_power_group_suppression.log`; upload commit `edc71a113381c0551ce72cfe3f38a1f22c363bb7`; User runtime observation 2026-09-13 that the two attacks using the Power animation did not damage. | Preserve selective early Power raw55 suppression as the proven research gate; do not return to whole-callback suppression or compensate with StatePosition writes. The next smallest causal question is authored Power FIST activation: after the proven early suppression and native `StatePosition 0 -> 1`, can the exact authored FIST event request the exact RIGHT PhysicalFist/raw55 source `5 -> 7` at marker time while Gothic retains contact/damage/cleanup ownership? Test physical activation alone first, without `ClearTriggeredList`, so activation and rearm remain separately proven responsibilities. Do not generalize to Sprint/Action9 or promote permanent raw55 architecture yet. |
| EV-276 | The true raw55 Power authored-FIST activation probe proves that marker-time physical activation alone is sufficient to restore native first-contact damage in the tested Power context; no probe-issued trigger-list rearm is required. Two factual Troll true-Power executions, C1 generations 6 and 11, first repeat the EV-275 gate: `CORE RAW55_POWER_GROUP_SUPPRESSION` suppresses the exact RIGHT `TrollFist` / PhysicalFist raw55 request from group5 to requested group7 at `StatePosition=0`, and the immediately enclosing original `_AI_PowerAttack` callback still returns `StatePosition=0->1`, `RightGroup=5->5`, `SameC1=1`, `SameRight=1`, `NativeResult=1`. At the later authored `G3AB_COL_FIST`, stable marker processing remains `UNSUPPORTED_MISSING_SOURCE`, but the diagnostic activation authorized by that same actor/source/C1 suppression proof requests the exact RIGHT raw55 source `5 -> 7` through the ordinary setter at `StatePosition=1`. Both interventions log `GroupBefore=5`, `RequestedGroup=7`, `GroupAfter=7`, `EarlySuppressionProof=1`, `ClearTriggeredList=0`, `ACTIVATE_FIST=1`; the ordinary `CORE COLLISION_GROUP` and `C1 OFFENSE REQUEST` records confirm a real physical opening and real lifecycle obligation. Native `CORE ONDAMAGE` against `PC_Hero` follows in both Power executions, matching the User's visual observation that both damaged. Native cleanup later returns the exact RIGHT source `7 -> 5`, fulfills the C1 obligation, and both C1s finalize `Outstanding=0`, `Cleanup=1`, `PhysicalChanged=0`, `NO_OP_NO_OUTSTANDING` with no terminal repair. | CONFIRMED RAW55 TRUE POWER AUTHORED-FIST ACTIVATION SUFFICIENT WITHOUT PROBE `CLEARTRIGGEREDLIST`; NATIVE DAMAGE + CLEANUP PRESERVED | Activation implementation `0131f7a04d548f186ed9c8581e59e84a7b24b1f7`; runtime `research/raw/2026.09.13_troll_raw55_power_fist_activation.log`; runtime upload commit `83d2150defd967e379d3ae602f6a2936e941d1bd`; User observation 2026-09-13 that both factual Power attacks damaged. | For tested true Power, preserve the original native callback and its required StatePosition progression, suppress only the premature exact scoped raw55 `5 -> 7`, then activate the exact RIGHT PhysicalFist/raw55 source at authored FIST timing through the ordinary collision-group path. Do not add a Power-specific `ClearTriggeredList` merely by analogy with Quick: this probe proves it is not required for the tested first authored-FIST contact. Preserve native damage and native `7 -> 5` cleanup ownership. Do not generalize Power's no-rearm result to Quick, Normal, or Sprint. Power research is sufficiently closed to move to the remaining temporary Normal/Sprint whole-callback suppression boundaries before permanent raw55 responsibility design. |
