# Gothic 3 Animation Behaviors — Evidence Ledger: EV-291 Onward

**Status:** Canonical continuation after `EVIDENCE_LEDGER_286_ONWARD.md` EV-290  
**Date:** 2026-09-17

**ID range:** EV-291 onward

## Purpose

Continue the single global monotonic evidence namespace after EV-290. This split is storage/retrieval only; it does not create a new evidence namespace.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-291 | The raw55 Normal native trigger-clear suppression causal probe proves that the exact post-hit1 public `eCTrigger_PS::ClearTriggeredList()` ALL call from `Script_Game.dll + 0x386C6` is causally required for Normal's implicit second same-C1 damage opportunity in the tested route. In representative factual Normal C1=14, frame-1 FIST at `StatePosition=0 / StateTime ~= 0.041` opens exact RIGHT `TrollFist` raw55 `5 -> 7` and the existing marker-owned clear executes normally. First native `ONDAMAGE Target=PC_Hero` occurs while StatePosition is still 0; the player then appears exactly once in the aligned visited/count arrays with visit count1. Around `StateTime ~= 0.252`, the already-established native `7 -> 7` setter suppression fires, after which the exact RIGHT ALL-clear call is observed with caller `Script_Game.dll +0x386C6`, `PlayerPresent=1 / PlayerEntryCount=1 / PlayerVisitCount=1 / CountsAligned=1`. The new probe returns `SUPPRESS_CLEAR=1`; the original ALL clear is skipped. POST observation therefore remains `VisitedSize=1 / VisitedCountSize=1 / PlayerPresent=1 / PlayerVisitCount=1`. The original `_AI_Attack` callback still completes `StatePosition 0 -> 1` while RIGHT remains group7 and the player remains visited. At the later authored FIST around `StateTime ~= 0.606`, marker2 remains observational and `PlayerPresent=1 / PlayerVisitCount=1`; no second Normal `ONDAMAGE` occurs in that C1. Native exact RIGHT `7 -> 5` cleanup then occurs and C1=14 finalizes with no outstanding obligation. Additional Normal C1s, including C1=16 and C1=41, repeat the exact eligible caller/contact suppression and preserve the player visit across the skipped boundary. The User visually observed that only the first physical swing damaged them. | **ROUTE A CONFIRMED: THE EXACT NATIVE PUBLIC ALL CLEAR FROM `Script_Game.dll +0x386C6` IS CAUSALLY REQUIRED FOR NORMAL'S IMPLICIT SECOND SAME-C1 DAMAGE OPPORTUNITY IN THE TESTED ROUTE. SUPPRESSION PRESERVES HIT1, PLAYER-VISITED STATE, `SP0 -> 1`, MARKER2 OBSERVATION, NATIVE `7 -> 5` CLEANUP AND CLEAN C1 FINALIZATION, WHILE HIT2 DISAPPEARS.** | Diagnostic implementation `2839c8c271fa240b81ce5ccd203c34058870a5bb`; local build/live DLL SHA256 `0F2CBF35199D111C405E03E5AE824B0AC76BF47B06C1BF13A47098BCFFF6F830`; canonical source `research/archive/2026.09.16_troll_raw55_normal_native_trigger_clear_suppression.log`, original upload commit `056951242b549c0de690ef80714b0806c688ef28`, byte-identical archive commit `6e0e2335a0198232df945f1e5c77bfeb5fea8fc8`, Git blob `fa7736929a7e7e79871458455c74fe9cc2bd42d7`, source SHA256 `33C2B2F3E7732B3AB9BEB42C4ED6528D91E0F08F5782F498AF9F1F1A231D9D98`; POP-07 package `research/derived/2026.09.16_troll_raw55_normal_native_trigger_clear_suppression_large_log/`; User visual observation on 2026-09-16 that only the first swing damaged. | Close the Normal native-clear necessity question. The native clear is not merely correlated bookkeeping: it creates the second-contact opportunity in the tested Normal route. The next smallest one-variable causal probe may therefore preserve the exact native-clear suppression and allow marker2 to perform one replacement `ClearTriggeredList()` on the exact current RIGHT raw55 trigger. If hit2 returns while all other behavior remains healthy, authored marker2 rearm ownership is proven. Do not yet generalize this Normal mechanism to Power or Sprint, and do not promote `PhysicalFistProbe` into permanent architecture before the remaining family/repeated-contact work closes. |
| EV-292 | The raw55 Normal marker2 replacement-clear causal probe proves that the later authored Normal FIST can replace Gothic's suppressed native between-contact clear and own the second same-C1 contact rearm in the tested route. Representative factual Normal C1=3 starts with marker1 at `StatePosition=0 / StateTime ~= 0.041`, exact RIGHT `TrollFist` raw55 `5 -> 7`, and the existing marker-owned `ClearTriggeredList()`. First `ONDAMAGE Target=PC_Hero` occurs while StatePosition remains 0, after which the player is present exactly once with visit count1. At `StateTime ~= 0.258`, the existing EV-288 native `7 -> 7` suppression fires and the EV-291 exact native ALL-clear suppression fires for caller `Script_Game.dll +0x386C6`; POST state remains player-present/count1 and `_AI_Attack` completes `StatePosition 0 -> 1` with RIGHT still group7. At the later authored FIST around `StateTime ~= 0.604`, the exact marker2 replacement-clear gate passes with same C1/source, raw55 group7, `StatePosition=1`, two authored FIST markers, all prior proof/suppression flags set, and factual PRE `PlayerPresent=1 / PlayerEntryCount=1 / PlayerVisitCount=1 / CountsAligned=1`. The replacement ALL clear is observed from `Script_FrameCollisionTest.dll`, executes normally, and POST becomes `VisitedSize=0 / VisitedCountSize=0 / PlayerPresent=0 / PlayerEntryCount=0 / CountsAligned=1`. A later second `ONDAMAGE Target=PC_Hero` occurs in the same Troll C1 at StatePosition1. Native exact RIGHT `7 -> 5` cleanup then executes, the outstanding obligation reaches zero, and C1=3 finalizes `NO_OP_NO_OUTSTANDING`. Additional factual Normal C1s including C1=7 and C1=18 repeat the exact marker2 PRE visited -> POST absent replacement-clear transition; C1=7 also records the restored later `ONDAMAGE`. The User visually observed both physical swings connecting/damaging. | **ROUTE A CONFIRMED: AUTHORED NORMAL MARKER2 `ClearTriggeredList()` IS SUFFICIENT TO REPLACE THE SUPPRESSED NATIVE BETWEEN-CONTACT CLEAR AND CAUSALLY OWNS THE SECOND SAME-C1 CONTACT REARM IN THE TESTED ROUTE. HIT1, NATIVE-CLEAR SUPPRESSION, `SP0 -> 1`, MARKER2 PRE/POST CONTACT RESET, HIT2 RESTORATION, NATIVE `7 -> 5` CLEANUP AND CLEAN C1 FINALIZATION ARE ALL OBSERVED IN ONE FACTUAL ROUTE.** | Diagnostic implementation `f2ae82991bc1c1675c6e56c45b6f1bd267f093ee`; independent Normal Chat source review PASS; local build/deploy/startup PASS; built/live DLL SHA256 `98878AA9DE76CA04C9EEFFA07A9FD44C190E6711825231D2688FE1B75864B6E3`; canonical source `research/archive/2026.09.17_troll_raw55_normal_marker2_replacement_clear.log`; source upload + derived-package commit `7e7c80b02541efd81a9cd03a882b4c9034bb2b62`; byte-identical archive commit `a4bda586d3d551a2525466ef9423fe00d41a75a3`; source Git blob `8e9edd5d9850efcf4caeee109101f4fab866ebe9`; source SHA256 `4AE9700C2944FC2940AB194E018478B88EF00A90157BC8DC3AC1FDB48BEDD91D`; source bytes `364338`; source lines `1636`; POP-07 package `research/derived/2026.09.17_troll_raw55_normal_marker2_replacement_clear_large_log/`; User visual observation on 2026-09-17 that both swings connected/damaged. | Close the Normal repeated-FIST contact-rearm question for the tested raw55 route. Normal's authored ownership is now causally understood: marker1 opens group7 and clears contact bookkeeping for hit1; with Gothic's hidden between-contact clear suppressed, marker2 can explicitly clear contact bookkeeping and restore hit2 without another group transition. Do not yet generalize the exact Normal timing/state route to Power or Sprint. Next responsibility is Normal Chat design/freeze for the smallest Power repeated-FIST rearm probe, followed by Sprint repeated-FIST rearm, before permanent raw55 architecture is designed. |
| EV-293 | The raw55 true-Power repeated-FIST rearm causal probe proves that, after the first authored Power FIST has already opened the exact RIGHT PhysicalFist/raw55 source to group7 and produced native first-contact damage, the later authored Power FIST can create the second same-C1 contact opportunity using only one `TouchDamage.ClearTriggeredList()` and no second collision-group request. Representative factual Power C1=6 begins with the EV-275 gate: the exact premature RIGHT raw55 `5 -> requested7` mutation is suppressed at `StatePosition=0 / StateTime ~= 1.403`, while the original `_AI_PowerAttack` callback still returns `StatePosition 0 -> 1`, `RightGroup=5 -> 5`, `SameC1=1`, `SameRight=1`. The first authored FIST then arrives at `StatePosition=1 / StateTime ~= 1.445`; EV-276 behavior performs one real exact RIGHT `5 -> 7` request, records `ClearTriggeredList=0`, creates one C1 offense obligation, and native `ONDAMAGE Target=PC_Hero` follows around StateTime 1.65. At the later authored FIST around `StateTime ~= 2.007`, the same C1/source remains factual `Action=2 / Family=POWER`, RIGHT is already group7, and the controlled decision has exactly two FIST markers. The repeated-FIST gate proves `EarlySuppressionProof=1 / FirstActivationUsed=1` and exact trigger PRE state `PlayerPresent=1 / PlayerEntryCount=1 / PlayerVisitCount=1 / CountsAligned=1`; one diagnostic `ClearTriggeredList()` executes, with POST `PlayerPresent=0 / PlayerEntryCount=0 / CountsAligned=1`. A later second native `ONDAMAGE Target=PC_Hero` occurs in the same Troll C1 around StateTime 2.13. Gothic then performs native exact RIGHT `7 -> 5` cleanup, fulfills the single outstanding offense obligation, and C1=6 finalizes `Outstanding=0 / Cleanup=1 / NO_OP_NO_OUTSTANDING`. Independent factual Power C1=52 repeats the same first activation, first damage, second-FIST PRE visited -> POST absent clear, second damage, and native cleanup sequence. The User visually observed at least two attacks using the shared Power/Sprint animation in which both swings connected and damaged; because Power and Sprint use the same animation, family identity is taken from the factual `Action=2 / Family=POWER` runtime records rather than appearance. | **ROUTE A CONFIRMED: TRUE POWER'S ALREADY-OPEN RAW55 SOURCE NEEDS NO SECOND GROUP REQUEST; ONE AUTHORED SECOND-FIST `ClearTriggeredList()` IS SUFFICIENT TO REARM CONTACT AND RESTORE A SECOND SAME-C1 NATIVE DAMAGE OPPORTUNITY IN THE TESTED TWO-FIST ROUTE. FIRST ACTIVATION, FIRST DAMAGE, PRE VISIT, AUTHORED CLEAR, POST ABSENCE, SECOND DAMAGE, NATIVE `7 -> 5` CLEANUP AND CLEAN C1 FINALIZATION ARE ALL PRESENT IN FACTUAL ACTION2 POWER EXECUTIONS.** | Diagnostic implementation `d3388d5c38c29ea4453259e9efb3fd67916d865e`; independent Normal Chat source review PASS; local build/deploy/startup PASS; built/live DLL SHA256 `1EAD1E361755368DF9DAC5C8A068549CFC63BDD95381247F41F1B4BA68693B7E`; canonical source `research/archive/2026.09.17_troll_raw55_power_repeat_fist_rearm.log`; source upload + derived-package commit `44abff33303662e7620b5bb961758b9f0a8bedfc`; byte-identical archive commit `5c5617cfdb38f45fb06d326cad722fb6e8a123a7`; source Git blob `ffa3777e9a9311afc77af2ffff4e84eb05fa6cdc`; source SHA256 `61F572B13D78C25A7363FC85076AEC7FAEC48CD8A51EC540F012D7541D1C42D5`; source bytes `1093172`; source lines `4945`; POP-07 package `research/derived/2026.09.17_troll_raw55_power_repeat_fist_rearm_large_log/`; User visual observation on 2026-09-17 that at least two attacks using the Power/Sprint-shared animation damaged on both swings. | Close Power repeated-FIST contact rearm for the tested raw55 route. Power authored ownership is now: first FIST performs the one physical `5 -> 7` opening with no probe clear; later FIST performs contact-bookkeeping clear only while the source remains group7. Do not interpret factual `Action=2` at a second marker as proof that every PowerAttack-named two-FIST execution originated as true Power: EV-283 proves Sprint-origin C1s can transition `Action9 -> Action2` before their second marker. The next responsibility is Sprint repeated-FIST rearm preserving same-C1 Sprint-origin ownership across that transition; permanent raw55 architecture remains paused until Sprint closes. |
