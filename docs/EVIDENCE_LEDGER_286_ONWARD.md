# Gothic 3 Animation Behaviors — Evidence Ledger: EV-286 Onward

**Status:** Canonical continuation after `EVIDENCE_LEDGER_283_ONWARD.md` EV-285  
**Date:** 2026-09-16

**ID range:** EV-286 onward

## Purpose

Continue the single global monotonic evidence namespace after EV-285. This split is storage/retrieval only; it does not create a new evidence namespace.

| ID | Statement | Status | Provenance / note | Engineering consequence |
|---|---|---|---|---|
| EV-286 | The raw55 Normal pre-StatePosition FIST probe proves that the first authored Normal FIST is not ignored and does not wait for Gothic's ordinary Normal timer to mutate the physical source. In representative factual Normal C1s 19, 21 and 23, the frame-1 `G3AB_COL_FIST` is transported while `Action=1 / Family=NORMAL / StatePosition=0`, with valid C1 and exact RIGHT `TrollFist` / PhysicalFist/raw55 still group5. The new probe immediately requests exact RIGHT `5 -> 7`, verifies group7, creates a real C1 offense obligation, and logs `ClearTriggeredList=0`. The source then remains group7 while the original `_AI_Attack` callback continues. Around `StateTime ~= 0.25`, Gothic independently requests `7 -> 7` and advances `StatePosition 0 -> 1`; this native transition is not suppressed. The later frame-15 FIST is also transported in the same Normal C1 at `StatePosition=1` while RIGHT remains group7, but the current Normal research path performs no repeat intervention. In C1=19 the second FIST is recorded around StateTime `0.604`, while the single observed `ONDAMAGE Target=PC_Hero` occurs later around StateTime `0.716`; C1=21 and C1=23 repeat the same broad pattern, each producing one later damage event and then native exact RIGHT `7 -> 5` cleanup with clean C1 finalization. No Normal damage is observed before Gothic's native `7 -> 7 / StatePosition 0 -> 1` transition. The User independently ran the fixture three times and consistently observed only one damaging contact per Normal attack, despite using the same two-swing animation geometry that visibly supports two hits under Quick. | **CONFIRMED NORMAL SP0 MARKER TRANSPORT + EXACT RAW55 `5 -> 7` PHYSICAL OPENING + PRESERVED NATIVE `7 -> 7 / SP0 -> 1` PROGRESSION + NATIVE CLEANUP; EARLY FIRST-SWING DAMAGE DOES NOT OCCUR WITH `ClearTriggeredList=0`. THE REMAINING UNKNOWN IS CONTACT/ELIGIBILITY, NOT MARKER ACCEPTANCE OR SOURCE MUTATION.** | Diagnostic implementation `a99f4aa616c905c4eb66c72bdaa27709c9b75c77`; local build/deploy SHA256 `575D1E14C60C1C80E1116296240BC731C37EE5EA3327574425C6EF33F2B8BAE0`; canonical raw `research/raw/2026.09.15_troll_raw55_normal_prestate_fist.log`, upload commit `1f336c04173983428e5e95a348207040c2fe9a14`, SHA256 `5E081A79BCB39280A738C5FBA97E6C4FF4E5DCFB22479FA8D8C16407AA7EC349`; POP-07 package `research/derived/2026.09.15_troll_raw55_normal_prestate_fist_large_log/`; User visual observations from three runs on 2026-09-15. | Close the Normal pre-state **marker-order/source-opening** question: the early authored marker is authoritative for physical raw55 opening and does not need to wait for the native Normal timer. Do not describe the result as marker rejection. The next one-variable causal test is contact rearm: keep the successful SP0 `5 -> 7` unchanged and add exactly one exact `TouchDamage.ClearTriggeredList()` immediately after verified group7, still before Gothic's native Normal transition. If damage then occurs before the native `7 -> 7 / SP0 -> 1`, triggered-list rearm is the missing early-contact primitive. If not, isolate an additional native Normal state/timing eligibility gate. Do not add second-marker Normal rearm, force StatePosition, or add damage code in the same probe. |
| EV-287 | The raw55 Normal pre-state contact-rearm probe proves that one exact `TouchDamage.ClearTriggeredList()` immediately after the authored frame-1 SP0 opening is sufficient to create a real first-contact opportunity **before** Gothic's ordinary Normal timer/state transition. In factual Normal C1=3, frame-1 FIST arrives at `StatePosition=0` around `StateTime=0.042`, opens exact current RIGHT `TrollFist` / PhysicalFist raw55 `5 -> 7`, verifies group7, and records `ClearTriggeredList=1 / PRESTATE_REARM=1`. A native `ONDAMAGE Target=PC_Hero` then occurs in the callback neighborhood around `StateTime ~= 0.188` while the Normal callback is still at `StatePosition=0`. Gothic does not reach its ordinary native Normal `7 -> 7` request and `StatePosition 0 -> 1` transition until around `StateTime ~= 0.257`. Later in the **same C1**, the authored frame-15 FIST is transported around `StateTime ~= 0.603` while RIGHT remains group7. The current Normal probe performs **no second-marker intervention**, yet a second native `ONDAMAGE Target=PC_Hero` occurs later around `StateTime ~= 0.723`. Gothic finally performs native exact RIGHT `7 -> 5` cleanup and the C1 finalizes with no outstanding obligation. The User ran the fixture twice and visibly observed Normal attacks damaging the player twice in both runs, although the positioning/timing was substantially harder than with Quick. | **CONFIRMED NORMAL SP0 CONTACT REARM: EXACT RIGHT `5 -> 7` + ONE `ClearTriggeredList()` AT THE FRAME-1 FIST ENABLES FIRST DAMAGE WHILE STATEPOSITION IS STILL 0, BEFORE THE NATIVE NORMAL TIMER. SAME-C1 SECOND DAMAGE ALSO OCCURS, BUT IT IS NOT YET PROVEN TO BE OWNED BY FRAME-15 FIST BECAUSE CURRENT NORMAL CODE DOES NOTHING AT THAT MARKER.** | Diagnostic implementation `50db909b414eb821b536cd68baa75efbf5589c1b`; local build/deploy SHA256 `627BADF62B1B4DA62781DFEF2625D760D83C63DC95E4F17FEE87264F456B5068`; canonical raw `research/raw/2026.09.15_troll_raw55_normal_prestate_rearm.log`, upload commit `06a9fb5875a3c78edc596638f3d019e3e3d87852`, SHA256 `73DB5D9ED77B6121FF01157A6973433CFCB7305C6911FA37AB80904D904C33C6`; POP-07 package `research/derived/2026.09.15_troll_raw55_normal_prestate_rearm_large_log/`; User visual observations from two runs on 2026-09-15. | Close the Normal first-contact primitive: a pre-state authored FIST needs exact-source physical opening plus triggered-list rearm when it occurs before Gothic's native Normal transition. Do **not** yet credit the later frame-15 FIST with the second damage opportunity. The native exact RIGHT `7 -> 7 / SP0 -> 1` transition occurs after the first contact and before marker 2, making the nested native `7 -> 7` setter the strongest current candidate for an implicit second rearm. Test that candidate causally by suppressing only that one same-C1 exact-source native `7 -> 7` request after proven SP0 rearm while preserving the original `_AI_Attack` callback and `SP0 -> 1` progression. If the second hit disappears, later production design must prevent native timer-owned rearm from substituting for authored marker occurrence; a separate marker-2 rearm can then be tested. If the second hit persists, isolate another native contact/state mechanism. |
| EV-288 | The raw55 Normal native-rearm-source causal probe rejects the hypothesis that Gothic's later exact RIGHT native `Item_Attack/group7 -> Item_Attack/group7` setter request is required for the observed second same-C1 damage opportunity. In representative Normal C1=14, frame-1 FIST at `StatePosition=0 / StateTime ~= 0.064` opens exact RIGHT `TrollFist` raw55 `5 -> 7` and performs `ClearTriggeredList=1 / PRESTATE_REARM=1`. A first `ONDAMAGE Target=PC_Hero` follows around the callback neighborhood at `StateTime ~= 0.193` while StatePosition remains 0. At `StateTime ~= 0.253`, the new one-shot `RAW55_NORMAL_NATIVE_REARM_SUPPRESSION_PROBE` factually suppresses the exact same-C1/same-RIGHT native request with `RequestedGroup=7 / BeforeGroup=7 / PreStateRearmProven=1 / NATIVE_7TO7_SUPPRESS=1`. The original `_AI_Attack` callback still advances `StatePosition 0 -> 1` in that same invocation while RIGHT remains group7. The authored frame-15 FIST is later transported around `StateTime ~= 0.601` at SP1 while RIGHT remains group7 and the current Normal probe still performs no marker-2 rearm. Despite the native `7 -> 7` setter having been suppressed, a second `ONDAMAGE Target=PC_Hero` occurs around `StateTime ~= 0.723`. Native exact RIGHT `7 -> 5` cleanup then occurs and C1=14 finalizes with no outstanding obligation. The User repeatedly observed both physical swings damaging the player during the run. | **HYPOTHESIS REJECTED: THE NATIVE NORMAL `7 -> 7` COLLISION-GROUP SETTER IS NOT CAUSALLY REQUIRED FOR THE SECOND SAME-C1 DAMAGE OPPORTUNITY. SUPPRESSING IT PRESERVES FIRST DAMAGE, `SP0 -> 1`, SECOND DAMAGE, NATIVE CLEANUP AND CLEAN C1 FINALIZATION. THE REMAINING SECOND-CONTACT OWNER IS SOME OTHER NATIVE TOUCH/TRIGGER/STATE MECHANISM.** | Diagnostic implementation `6ca48c84e9fdf370ffdb824f9142e8ef0e267a7c`; local build/live DLL SHA256 `DA392693066AD9CAF3E52789E0115592DC4E08B43CAA4DBA0B4E0C55E8473784`; canonical raw `research/raw/2026.09.16_troll_raw55_normal_native_rearm_source.log`, upload commit `9af56b93cafbddbe4b645bc574439758ccc2edcc`, SHA256 `AB8E7B8B300F08CD61B01E7338FC672E6A09DE7D57F055F4FFAFD68514628C6E`; POP-07 package `research/derived/2026.09.16_troll_raw55_normal_native_rearm_source_large_log/`; User visual observation on 2026-09-16 that both swings damaged several times across the run. | Close the native-group-setter hypothesis. Do **not** infer that `StatePosition 0 -> 1` itself rearms contact: that transition survived the probe but was not independently manipulated. Before another causal mutation, inspect the exact RIGHT raw55 `eCTrigger_PS` bookkeeping directly. The official SDK exposes read-only `EntitiesVisited` and `EntitiesVisitedCount`, while `gCTouchDamage_PS` exposes `ResetOnUntouch`; use those facts to observe whether the player is added after hit 1 and later removed/rearmed naturally before hit 2 while the native `7 -> 7` setter remains suppressed. Prefer change-only observation within `PhysicalFistProbe`; do not add another global hook merely to inspect this state. |
| EV-289 | The raw55 Normal read-only trigger-state observation proves that the prior player-contact bookkeeping is reset **inside Gothic's native Normal `StatePosition 0 -> 1` callback transition and before the later authored FIST**, while the EV-288 native `7 -> 7` collision-group setter remains suppressed. In representative Normal C1=11, immediately after the frame-1 authored `5 -> 7 + ClearTriggeredList()` rearm at `StateTime ~= 0.058`, the exact RIGHT `TrollFist` raw55 trigger reports `ResetOnUntouch=0`, `VisitedSize=0`, `VisitedCountSize=0`, and `PlayerPresent=0`. The first `ONDAMAGE Target=PC_Hero` occurs around `StateTime ~= 0.204`; the subsequent change-only snapshot around `0.213` shows `VisitedSize=1`, `VisitedCountSize=1`, `PlayerPresent=1`, `PlayerEntryCount=1`, `PlayerVisitCount=1`. Immediately before the exact native `7 -> 7` suppression around `0.259`, the player is still present with the same count. The setter is then suppressed, yet at `SP0_TO1_POST_CALLBACK` in that **same original `_AI_Attack` invocation**, RIGHT remains group7 while the trigger arrays are already empty again: `VisitedSize=0`, `VisitedCountSize=0`, `PlayerPresent=0`, with `ResetOnUntouch=0` unchanged. A later callback can contain another visited entity while `PC_Hero` remains absent. At the later same-C1 authored FIST around `StateTime ~= 0.600`, the player is still absent from the visited set and the marker performs no Normal repeat intervention. A second `ONDAMAGE Target=PC_Hero` occurs around `0.737`, after which the player is observed inserted again with `PlayerVisitCount=1`. Native exact RIGHT `7 -> 5` cleanup and clean C1 finalization follow. The User visually observed both swings damaging the player. | **CONFIRMED NATIVE NORMAL TRIGGER-BOOKKEEPING RESET BETWEEN CONTACTS: AFTER HIT 1 THE PLAYER IS VISITED; DURING THE NATIVE `SP0 -> 1` CALLBACK TRANSITION THE VISITED STATE IS CLEARED BEFORE MARKER 2, EVEN WITH THE `7 -> 7` SETTER SUPPRESSED AND `ResetOnUntouch=0`; HIT 2 THEN RE-INSERTS THE PLAYER. THE EXACT NATIVE CLEAR/RESET OPERATION IS NOT YET IDENTIFIED.** | Diagnostic implementation `ec20e769a347bd206577427bfd28a85bd51a79b3`; local build/live DLL SHA256 `D336619855E4812EA950797057D57BC44D41F141DA244D209880793C0ED69367`; canonical raw `research/raw/2026.09.16_troll_raw55_normal_trigger_state_observation.log`, upload commit `f486da9cf48e10d06b7c7040f3393b1d9a54cd52`, SHA256 `978817C03EE01EA3098D265CFE85544F4D3DE06019279AD04C337C4ADBE00D08`; POP-07 package `research/derived/2026.09.16_troll_raw55_normal_trigger_state_observation_large_log/`; User visual observation on 2026-09-16 that both swings damaged. Static follow-up: `Script_Game.dll` imports `PSTouchDamage::ClearTriggeredList()`; tested `Script.dll` exports that wrapper at `+0x13720`, where disassembly shows it tail-jumps to the engine property-set clear; tested `Engine.dll` exports `eCTrigger_PS::ClearTriggeredList()` at `+0x7DDA0` and `eCTrigger_PS::ClearTriggeredList(eCEntity*)` at `+0x7DDF0`. | Close the read-only bookkeeping question: the implicit second Normal contact opportunity is created before marker 2 by a native reset during the `SP0 -> 1` callback transition, not by marker 2, not by the suppressed `7 -> 7` setter, and not by observed `ResetOnUntouch` behavior (`ResetOnUntouch=0`). Do **not** infer that StatePosition itself performs the reset. The next smallest diagnostic is to observe the two exact public engine `ClearTriggeredList` overloads for the exact RIGHT raw55 trigger, preserving all current behavior and logging caller module/RVA. If a post-first-hit clear call empties the player visit state, that identifies the factual reset operation/caller. If neither public clear API fires while the arrays still reset, rule them out and continue into lower trigger internals rather than guessing. |
